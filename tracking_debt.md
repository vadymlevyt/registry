# tracking_debt.md — Реєстр свідомо відкладеного

Живий реєстр рішень «свідомо НЕ робимо зараз». Механізм золотої середини
(DEVELOPMENT_PHILOSOPHY.md, розділ «ПРИНЦИП ЗДОРОВОГО ОРГАНІЗМУ»): чистимо/будуємо
тільки те, що активується поточним TASK; решта — сюди, з **явним тригером**.

**Правило входу:** запис без конкретного тригера (подія, не «колись») — невалідний.
Це не борг, а забування. Тригер має бути перевіряємим.

| # | Що відкладено | Чому свідомо не робимо зараз | Явний тригер активації | Джерело / посилання |
|---|---------------|------------------------------|------------------------|---------------------|
| 1 | Backfill denormalized `case.client` (string) і `proceeding.judges` (string) із `parties[]` / `composition` | v7 ввів канонічні `parties[]`/`composition`; UI ще читає старі summary-поля. Переписувати UI зараз — поза обсягом інфраструктурного v7. | Коли реальний канал (Court Sync) почне писати `parties[]`/`composition` і UI треба показувати дані з них → окремий backfill TASK (читає parties → генерує summary, UI не змінюється до того). | ARCHITECTURE_HISTORY.md § «Canonical Schema v7 → Tracking debt»; `report_task_0_3_5_canonical_schema_v7.md` |
| 2 | Перевірка повноти консолідованого дайджесту після виносу історії | Винос CLAUDE.md → ARCHITECTURE_HISTORY.md зробив дайджест ручним консолідатом 8 версійних секцій; ризик — пропущена активна константа/enum при наступній зміні схеми. | При наступному schema bump (v7→v8+): перед редагуванням дайджесту звірити його enum/ACTIONS/PERMISSIONS з кодом і з ARCHITECTURE_HISTORY.md, переконатися що нічого активного не загубилось. | Цей TASK (розвантаження CLAUDE.md, 2026-05-15) |
| 4 | Косметичні текстові згадки видаленого `DocumentProcessor` у коментарях/рядках: `documentFactory.js:3,200`, `toolDefinitions.js:55`, `migrations/v4ToV5.js:43`, `caseSchema.js:78` (опис `lastProcessingContext`). | Старий компонент видалено (TASK 1), але посилання в коментарях/описах лишились. Масово правити зараз — поза scope (не функціональні, не ламають, ризик зачепити зайве). | Коли кожен із цих файлів наступного разу редагується по суті — прибрати/уточнити згадку в тому ж коміті. | TASK 1 salvage-and-decommission (2026-05-15) |
| 5 | Розбіжність clamp-логіки нарізки: legacy мав два майже-дублікати — `splitPDFByDocuments` (`startIdx = doc.startPage - 1`) і inline у `handleSplit` (`Math.max(0, doc.startPage - 1)`). Трансплантовано перший дослівно (`documentBoundary/splitPdf.js`); другий зник разом зі старим DP. | Salvage-TASK переносить дослівно, не зливає дві сутності тихо (правило #11). Захист від негативного startIdx — рішення архітектури DP v2, не salvage. | Коли DP v2 підключатиме `documentBoundary` — свідомо обрати канонічний clamp (ймовірно `Math.max(0, …)`) і покрити тестом. | TASK 1; `docs/reports/report_task_1_salvage_and_decommission.md`; pre-deletion код — git tag `pre-dp-v2-old-dp-removal` (на remote — SHA `b3b847f`) |
| 6 | Doc-drift: задокументований у CLAUDE.md/спеках перелік значень `time_entry` capture-method (`timer\|manual\|agent\|import\|legacy`) ≠ фактичні значення в коді (`instrumentation`, `manual_assign`, `manual`, `legacy_import`, + те що передає caller через `context.captureMethod`). | TASK 2 — це rename ПОЛЯ, не нормалізація значень; правити документований enum = окремий doc-TASK (CLAUDE.md заборонено чіпати в TASK 2). Перелік значень не валідується міграцією свідомо (валідація проти хибного enum зіпсувала б легітимні значення). | Окремий doc-sync TASK для CLAUDE.md (синхронізація задокументованих enum зі станом коду) — там виправити перелік. | TASK 2; `docs/reports/report_task_2_time_entry_capture_method.md` |
| 7 | `ACTIONS.add_time_entry` має мертвий рядок `const tenant = getCurrentTenant ? null : null;` — `getCurrentTenant` ніде не імпортовано (ні в колишньому App.jsx, ні в `actionsRegistry.js`). При реальному виклику `add_time_entry` це кидає ReferenceError, перехоплюється `executeAction` try/catch → `{success:false}`. `tenant` ніде не використовується (`tenantId` рахується з `getCurrentUser()`). | Латентний баг ще з App.jsx; TASK 5 — behavior-preserving винос, рядок збережено дослівно (виправлення = зміна поведінки, поза scope; інструкція TASK 5 прямо вимагає фіксувати, не виправляти). Жоден тест не виконує `add_time_entry` (тому baseline зелений). | Коли `add_time_entry` буде підключено до реального caller'а АБО прийде перший фейл-репорт по `add_time_entry` → видалити мертвий рядок (значення `tenant` не потрібне) окремим bug-fix TASK. | TASK 5 mini-аудит; `docs/reports/report_task_5_actionsregistry_refactor.md`; `actionsRegistry.js` (колишній `App.jsx:5108`) |
| 8 | Неконсистентність форми результату ACTIONS: `update_case_field` на забороненому полі повертає `{ error }` БЕЗ `success:false`, тоді як сусідні ACTIONS (`add_document`, `add_hearing`, …) повертають `{ success:false, error }`. Колишній `_actionsHarness` маскував це (додавав `success:false` від себе). | TASK 5 — behavior-preserving; нормалізація форми = зміна поведінки App, поза scope (інструкція TASK 5: дрібну неконсистентність фіксувати в tracking_debt, не виправляти). Асерт у `drag-n-drop.test.js` вирівняно на фактичну форму (`toBeFalsy`), інтент незмінний. | Окремий ACTIONS-return-shape normalization TASK (або наступного разу коли `update_case_field` редагується по суті) → стандартизувати на `{ success:false, error }` + покрити тестом. | TASK 5; `docs/reports/report_task_5_actionsregistry_refactor.md` |
| 9 | `prepareFile` (`CaseDossier/index.jsx` ~1525) після видалення drop-queue (DP-1) референсований лише мертвим блоком `{false && (…)}` старої inline-модалки. Лексично «вживається» (нема lint-помилки), фактично dead code що підтримує dead code. | DP-1 видалив drop-queue (рішення адвоката); стара inline-модалка має **власний** запланований cleanup TASK (її коментар «буде видалена окремим cleanup TASK»). Чистити `prepareFile` зараз = торкатись того `{false}` блоку = scope creep у свідомо відкладене (DEVELOPMENT_PHILOSOPHY: чистимо лише те що активує цей TASK). | Коли спрацює cleanup-TASK старої inline-модалки (видалення `{false && (…)}` блоку) — прибрати `prepareFile` тим самим комітом (HEIC-pre-step там більше нікому не потрібен; конвертацію HEIC покриває `converterService`). | TASK DP-1; `docs/reports/report_task_dp1_pipeline.md` |
| 10 | `tests/integration/drag-n-drop.test.js` — назва історична після видалення drop-queue UI (DP-1). Тест НЕ імпортує компонент: перевіряє `add_document` ACTION-контракт (null category/author/procId → ⚠-маркер `needsReview`) через `_actionsTestSetup` — валідний і зелений (AddDocumentModal/DP v2 теж покладаються на цей контракт). | Перейменування зараз = ризик без потреби (green; тестує живий ACTION-контракт, не видалений UI). Видалення drop-queue його не торкається (DP-1 підтверджено прогоном). | Наступне редагування цього файлу по суті → перейменувати у `add_document_minimal_metadata.test.js` (відв'язати назву від видаленого UI) + оновити шапку-коментар (стале посилання `CaseDossier:1942-1985`). | TASK DP-1; `docs/reports/report_task_dp1_pipeline.md` |
| 11 | Латентна #11-колізія: `tenant.settings.ecitsAutoProcess` (enum `'manual'\|'auto'`, активний свіч DP-2, читається `getEcitsAutoProcess()` + `ecitsInboxWatcher`) і пре-існуючий `tenant.settings.moduleIntegration.ecits.autoProcessIncoming` (boolean, TASK 0.2 інфра-плейсхолдер, **нуль споживачів** у коді) — два імені про один намір «чи авто-обробляти ЄСІТС-надходження». | DP-2 свідомо НЕ зливає: `autoProcessIncoming` — placeholder без жодного caller'а (та сама speculative generality що `discussion_dp_v2_philosophy_response` §Q3 «нема caller → запис у tracking_debt, не структура»). Конвергенція зараз = чистити те, що цей TASK не активує (золота середина). DP-2 додав канонічний `ecitsAutoProcess` як прямо вимагає handoff §4.5. | Коли `moduleIntegration.ecits.autoProcessIncoming` отримає першого реального споживача АБО DP-4 робитиме settings-UI ЄСІТС → звести два поля в одне (лишити `ecitsAutoProcess`, прибрати boolean-placeholder) тим же TASK. | TASK DP-2; `docs/reports/report_task_dp2_archives_ecits.md`; `tenantService.js` (`DEFAULT_TENANT.settings`) |
| 12 | Debug `console.log("executeAction OK: …", params, result)` у гарячому шляху реального `executeAction` (`actionsRegistry.js:1678`) — шумить у виводі будь-якого інтеграційного тесту що ганяє справжній `createActions` (зокрема нового `dp2-stages.test.js`). | Пре-існуючий ще з App.jsx (TASK 5 виносив behavior-preserving — рядок збережено дослівно). DP-2 його НЕ вводив і НЕ чистить (поза scope; «чисти лише те що активує цей TASK»; behavior-neutral — лише console-шум, не логіка). | Окремий debug-cleanup TASK для `actionsRegistry.js` АБО наступне редагування `executeAction` по суті → прибрати/огорнути в debug-gate цей `console.log` (і парний `:1226`). | TASK DP-2; `docs/reports/report_task_dp2_archives_ecits.md`; `actionsRegistry.js:1678` |
| 13 | `DocumentPipelineProvider`: `executor` `useMemo` залежить від `executeAction` (новий референс щорендер App, бо `createActions` живе в тілі App — TASK 5) → `streamingExecutor` пересоздається щорендер App. | Behavior-neutral: in-flight `run()` тримає старе замикання; `jobProgressStore` модульний (прогрес не губиться); `cancelledRef`/`runOptionsRef` стабільні `useRef`. Виправлення (стабілізація референсу `executeAction`/мемоізація поза App) — зміна DI-каркасу, поза scope BUGFIX. | Профілювання продуктивності DP на великому пакеті виявить накладні витрати АБО окремий TASK стабілізації `executeAction`-референсу → мемоізувати executor поза залежністю від `executeAction`-ідентичності. | DIAGNOSTIC DP-4 bugfix (2026-05-18); `docs/diagnostics/diagnostic_dp4_bugfix.md` §12 |
| 14 | Дедуп документів — metadata-евристика (`findDuplicate`: назва+pageCount/розмір), не контент-хеш (свідоме рішення адвоката: BUGFIX не додає поле/міграцію). Ризик хибно-позитивного авто-пропуску для різних документів з однаковою назвою+pageCount у різних прогонах (рідко; пом'якшено decision у «Потребує уваги»). | Контент-хеш потребує нового поля канонічної схеми + bump schemaVersion + ідемпотентної міграції — feature-розмір, прямо заборонено scope BUGFIX («НЕ робити нові фічі», «НЕ додавати поля без bump»). | Наступний schemaVersion bump → додати `contentHash` у канонічну схему + міграцію; замінити `findDuplicate` на хеш (точний дублікат → справжня автозаміна; варіант → інтерактивне «замінити/новий варіант» у DP-6). | DIAGNOSTIC DP-4 bugfix; `splitDocumentsV3.js findDuplicate`; `docs/diagnostics/diagnostic_dp4_bugfix.md` §12 |
| 15 | Перемикач PDF↔Текст у в'юері документа (CaseDossier): TXT з 02_ОБРОБЛЕНІ існує і локалізується наявним `ocrService.getCachedText` (конвенція `<basename>_<driveId>.txt`, зміна схеми НЕ потрібна), але UI його не показує поряд з PDF. | Окрема UI-задача іншого модуля (CaseDossier), не дефект потоку даних pipeline; змішувати UI-фічу в correctness-BUGFIX порушує «один TASK — одна турбота». Залежить від G1 (інакше перемикач показав би змішаний текст і виглядав би зламаним). | Тригер спрацював — G1 (page-precise TXT) у гілці BUGFIX. Окремий малий CaseDossier-TASK: кнопка PDF/Текст → `getCachedText(document)` (driveId+name), без зміни схеми. | Smart Triage BUGFIX G6 (2026-05-19); `docs/reports/report_smart_triage_bugfix.md`; `ocrService.js:51` |
| 16 | Інтеграція `cleanForReading` (Haiku-чистка для читання) зі slice не визначена: для нарізаних G1 пише СИРИЙ per-page `_text` (детерміновано, точні межі); чистка дає цілісний MD без меж сторінок — посторінкова нарізка несумісна. Зараз slice ігнорує чистку. | Адвокат НЕ вмикав чистку на прогоні Брановського (дефолт). Варіант 1 (завжди сирий per-page для slice) / 2 (чистити кожен зріз — N Haiku-викликів, дорого, підсилює bug 3) / 3 (без чистки для slice) свідомо відкладено доки сирий slicing не підтверджено на реальній справі. | **✅ РОЗЧИНЕНО TASK 3.1 (2026-06-01).** Нова філософія адвоката: очистка — НЕ до нарізки, а ПОСТ-КРОК на вже роз'єднаних фінальних документах (`splitDocumentsV3` після `writeProcessedArtifacts` → `cleanDocument` по кожному `scanned`-документу). Нарізка пише сирий per-page `.txt`/`.layout` (точні межі), потім очистка `.txt`→`.md` на готовому документі. Дилема «не можу різати MD по сторінках» зникає — конфлікту меж нема. Працює однаково для slice і merge. | Smart Triage BUGFIX G1/G6 (2026-05-19); РОЗЧИНЕНО — `docs/reports/report_task3.1_clean_text_core.md`; `splitDocumentsV3.js` пост-крок |
| 17 | HEIC→JPEG передобробка (або візуальний fallback) ПЕРЕД image_merge маршрутом. Симптом: `HTMLImage→createImageBitmap decode failed` на конкретних файлах (паспорт громадянина України Брановського, ймовірно HEIC або PDF з image-only сторінкою низької якості). B3-фікс уже робить graceful failure (документ → decision `image_merge_failed`, pipeline продовжує), але сам декодер не отримав fallback. | B3 закрив токсичність помилки (не валить інші документи). Додавання HEIC pre-step / альтернативного декодера = окрема feature-задача, не correctness-фікс. Поки симптом локалізований одним документом-паспортом — можна жити з graceful skip. | **Повторні `image_merge_failed` decisions на HEIC АБО PDF з image-only сторінкою** на різних справах (≥2 випадки) → окремий малий TASK: lazy-import `heic2any` як у конвертері, спроба декодування JPEG fallback, лише потім graceful skip. | DP layout-leak + speed fixes Phase A B3 (2026-05-20); `docs/reports/report_task_dp_layout_leak_and_speed_fixes.md`; `splitDocumentsV3.js` image_merge гілка |
| 18 | Подвійна риска прогрес-бару у `GlobalProgressScreen.jsx` під час фази PERSIST («Розкладання документів»). Візуальний side-effect, з'явився після фіксів B1-B3 (ймовірно нова гілка state-машини для decisions додала другий ProgressBar instance). Прогрес-індикація фактично працює (модалка у фоні теж показує лічильник «Документ N з M»), але візуально неохайно. | UI-cosmetic, не блокує роботу і не ламає функціональність. Виправлення зараз = scope creep у correctness-TASK. | Наступне редагування `GlobalProgressScreen.jsx` по суті АБО окремий UI-cleanup TASK (можна об'єднати з іншими дрібними UI-фіксами модалки DP в одному PR). | DP layout-leak + speed fixes Phase A (2026-05-20); `docs/reports/report_task_dp_layout_leak_and_speed_fixes.md`; `src/components/DocumentProcessorV2/GlobalProgressScreen.jsx` |
| 19 | Збагачений дайджест паспорта меж (D1 у `TASK_smart_triage_passport_scale_and_text.md` §3.2): печатка/підпис (`visualElements`), стрибок якості (`imageQualityScores` дельта vs попередня сторінка), розрідженість (мало blocks + короткий `_text`), дельта формату/орієнтації vs попередня, зміна мови. **Зафіксовано як D1, повністю не реалізовано.** Якість нарізки на Брановського ~85-90%, на 273-стор. крим. томі (Нестеренко, 2026-05-20) **~35% (26/74)** — тригер «<90%» **формально активований**. | Phase A швидкість/стабільність прийнятні на великих томах (273 стор. за 20 хв, без падінь). Але якість на великих провалена бо адаптивна щільність вимикає тіло тексту на >100 стор., а збагачений дайджест не реалізований — Triage гадає на коротких документах. | **АКТИВНО** — `TASK_dp_toc_and_enriched_digest.md` (готова спека), фази ФД-D1+D2+D3. Закриває цей борг + #21 разом. | `TASK_smart_triage_passport_scale_and_text.md` §3.2 D1; `diagnostic_dp_large_volume_test.md`; ФД-1.1 коміт `af79086` (часткова — адаптивна щільність, не D1) |
| 20 | **Phase B forward-fix** — P1 (паралелізація PERSIST через `runWithConcurrency`) спричинив регресію на реальному Provider'і (`vadymlevyt.github.io/registry/`, тест на планшеті 2026-05-20): branch A (slice) опинилась у passthrough → ціла справа одним документом, перемикач Скан/Текст зник. Швидкість при цьому виграла ×2 (2-3 хв замість 5-6). 5 revert-комітів повернули main на функціональний стан Phase A. **Phase B гілка `claude/optimize-dp-pipeline-phase-b-Sw2NH` (HEAD `7491b92`) збережена на origin** для пост-mortem і forward-fix. Гіпотези кореня: (а) dedup-race `registryView() ∪ pendingInBatch` хибно ловить нарізані як дублікати; (б) зміна error-contract (`throw` → `stageError` aggregation через `runWithConcurrency __error`) не виставила `stageError` на edge-case і branch A повернула `ok:true` з 0 documents. 1423 unit-тести цього не зловили бо integration тест P1 перевіряв concurrency-ліміт, а **не якісну поведінку 25-doc плану** через справжній Provider. | Phase A швидкість прийнятна (5-6 хв на 65pp, 20 хв на 273pp, якість — окрема проблема боргу #19). Phase B forward-fix потребує: (1) діагностика з реалістичним 25-doc планом і детермінованим mock Drive — звузити (а) vs (б); (2) виправити корінь у `splitDocumentsV3.js` (P1) і/або `concurrency.js`; (3) ОБОВ'ЯЗКОВИЙ **Provider-integration тест якісної поведінки** (інституційне обмеження №1 батьківського TASK_smart_triage) — після Provider-DP run з 25-doc планом усі 25 documents мають бути створені, branch A повністю виконана, без passthrough. P2-P4 окремо коректні і можуть бути перезапушені окремо в окремих фіксах. | Коли потрібна додаткова швидкість на великих 200-250-сторінкових томах (особливо з очисткою тексту / стисненням) + є час на діагностику з реалістичним Provider-integration тестом → окрема сесія Phase B v2 forward-fix. До того моменту Phase A швидкість прийнятна. | `docs/reports/report_task_dp_speed_phase_b_reverted.md`; revert-коміти `b6b8e62` `2a70949` `495ec56` `de12853` `88ddd09`; Phase B гілка `claude/optimize-dp-pipeline-phase-b-Sw2NH` HEAD `7491b92` |
| 21 | **Детектор реєстру/опису матеріалів справи** (як preferred маршрут Triage). В українській правовій практиці більшість томів справ починається з табличного реєстру: «№ \| Назва документа \| Аркуші». Зустрічається у кримінальних томах (майже завжди), цивільних з великими томами (часто), адміністративних провадженнях (часто). **Загальне правило, не специфічне для кримінальних.** На 273-стор. томі Нестеренка реєстр містив повний план на 74 документи — Triage його викинув разом з 47 реальними. AI має сам прочитати заголовок («Опис документів», «Реєстр матеріалів», «Перелік», «Зміст тома» тощо) і визнати реєстр. Парсити таблицю → ground truth, обходити AI Triage. Враховувати **зміщення нумерації** (опис на стор. 1-3 не входить у нумерацію документів — аркуш 1 у таблиці = фіз. сторінка 4). | Реєстр містить **детермінований план нарізки** — нема потреби в евристиках Triage. Це не «трохи покращити якість», це **обхід AI на готовому ground truth**. Реалізація — два Haiku-виклики (детекція + парс), окремий сервіс `tocDetector.js`, препроцесор у `triageStage` (не нова стадія, обмеження №4). | **АКТИВНО** — `TASK_dp_toc_and_enriched_digest.md` (готова спека), Гілка A (фази ФД-T1+T2). Закриває цей борг разом з #19. | `diagnostic_dp_large_volume_test.md` (273-стор. том Нестеренка дав 35% точність бо реєстр викинуто); `TASK_dp_toc_and_enriched_digest.md` §3 |
| 22 | Legacy `tenant.subscription.features = ['all']` — поряд з канонічним `entitlements`. Зараз: `features` НЕ читається жодним споживачем, але лишається в `DEFAULT_TENANT` і у tenant'ах з Drive. TASK 0.4 ввів структурований `entitlements` (per-module/scenario, з trial/expires/remaining), `features` помічений як DEPRECATED у коментарі схеми. | Видалення зараз = risky touch (теоретично якийсь майбутній dump/export може його читати; немає тригера що активує борг сьогодні). `entitlements` живе поряд — ДНК-додавання, не переписування. Жодний read-сайт на `features` не існує (grep чистий, аудит ✓). | Коли з'явиться UI білінгу (TASK Billing UI v1, через 6+ міс) АБО якщо `entitlements` стане єдиним джерелом правди в SaaS-комерціалізації → окремий cleanup TASK: prove no readers → видалити поле з DEFAULT_TENANT і додати міграцію `migrateToVersionX` що чистить існуючі tenants. | TASK 0.4 Court Sync MVP (2026-05-23); `src/services/tenantService.js:55` |
| 23 | `extensionBridge.registerExtension(name, version, capabilities)` API — handshake з підтвердженням провенансу майбутнього власного Chrome extension. У MVP TASK 0.4 свідомо НЕ закладено (YAGNI): зараз єдиний споживач — `submitScenarioResult` через `transport='extension'`, який саме по собі ідентифікує канал. Реальний handshake потрібен коли буде кілька extension-споживачів і/або enforcement тарифних обмежень на `apiLevel`. | Передчасна абстракція: один споживач, transport-маркер достатній; додавання `registerExtension` зараз = функція без caller'а (та сама помилка що `moduleIntegration.ecits.autoProcessIncoming` placeholder, борг #11). | Коли власне розширення Legal BMS буде розробляєтись (Track B) АБО з'явиться другий extension-споживач (third-party integration) → додати `registerExtension(meta)` у `extensionBridge.js`, оновити `apiLevel`, протестувати handshake. | TASK 0.4 Court Sync MVP; `src/services/extensionBridge.js` (коментар «НЕ закладаємо у MVP») |
| 24 | Catch-shape двійник на UPLOAD_FAILED early-return: `streamingExecutor.js:192-195` (помилка `drivePort.uploadBytes` оригіналу в `_temp`) повертає сингулярний `error: state.error` замість `errors:[…]`. Той самий симптом що чинив TASK EXECUTOR_THREW visible (Зона 3 «Помилки» → «Помилок немає» при реальному збою upload). НЕ catch-гілка — non-throw early-return всередині try, тому виправлений outer catch (рядки 307-321) її не охоплює. | Спека TASK явно обмежила скоуп рядками 307-315 (catch-гілка); правило «one TASK = one change». Тривіальна заміна того ж патерну, але формально окрема non-throw гілка з власним семантичним каналом (`UPLOAD_FAILED`). | Коли адвокат побачить у Зоні 3 реальну причину 4-chunk падіння і виявиться що це Drive 503/upload АБО наступне редагування `streamingExecutor.js` по суті → вирівняти UPLOAD_FAILED early-return на той самий shape (`stoppedAt, errors:[state.error], decisions:[]`) тим же комітом. Перевірити чи blocked-verdict (рядок 167) теж заслуговує вирівнювання — там окреме поле `blocked:true` і DPv2 має для нього свій тост; чіпати не треба. | TASK EXECUTOR_THREW visible (2026-05-26); `docs/reports/report_task_executor_threw_visible_in_zone3.md` §4 |
| 25 | `CATEGORY_OPTIONS` / `AUTHOR_OPTIONS` дублюються у `src/components/ImageEditor/constants.js` поряд з канонічним домом у `src/schemas/documentSchema.js` (`CANONICAL_DOCUMENT_FIELDS` enum). Споживачі (`ImageMergePanel/PreviewView.jsx` — TASK 1A; майбутній DP image-merge — TASK 1B) тягнуть label-pair з ImageEditor/. Канонічний enum схеми label'ів не містить — це окремий зріз (i18n-зрозумілий лейбл для адвоката), не сама схема. | Винос у `schemas/documentSchema.js` зараз = розширення scope TASK 1A (інакше треба зачіпати класифікацію в кількох місцях одночасно: `documentFactory.normalizeCategory`, `validateDocument`, DP UI). Спека TASK_image_merge_unify явно заборонила робити це в 1A («лише борг»). | Окремий backfill TASK класифікації документів (узгодження UI label'ів з enum схеми, можливо i18n-каталог) АБО наступний TASK що редагує `documentSchema.js` по суті → перенести `CATEGORY_OPTIONS`/`AUTHOR_OPTIONS` у `schemas/documentSchema.js` (поряд з enum), оновити імпорти в `ImageEditor/`, видалити дублікат з `ImageEditor/constants.js`. | TASK 1A image_merge_unify (2026-05-29); `docs/tasks/TASK_image_merge_unify.md` ПІД-TASK 1A; `src/components/ImageEditor/constants.js`; `docs/reports/report_task_image_merge_unify_1A.md` |
| 26 | CSS-класи у `src/components/ImageEditor/*` лишилися з префіксом `image-merge-panel__*` (BEM), хоча компоненти тепер «спільне місце» а не «частина модалки». Стилі підвантажуються `AddDocumentModal.jsx` глобально через `import './ImageMergePanel.css'`. Будь-який майбутній DP image-merge UI (TASK 1B) теж писатиме `image-merge-panel__*` класи — фактично reusable, ім'я модалкою-специфічне. | Перейменування зараз = широка одночасна зміна 9 файлів і CSS (поза scope behavior-preserving 1A). Спека 1A явно: «лише шляхи імпортів, не логіку». CSS namespace `image-merge-panel__*` працює коректно з обох споживачів (модалки і майбутнього DP) поки CSS файл завантажено хоч кимось. | TASK 1B (N-doc склейка в DP) АБО окремий cosmetic-TASK → перейменувати CSS-префікс `image-merge-panel__` → `image-editor__` у `ImageMergePanel.css` і у класах усіх файлів `ImageEditor/`; перейменувати файл CSS (`ImageMergePanel.css` → `ImageEditor.css`) і перенести у `src/components/ImageEditor/`; оновити `AddDocumentModal.jsx` імпорт. | TASK 1A image_merge_unify (2026-05-29); `src/components/CaseDossier/ImageMergePanel.css`; `src/components/ImageEditor/*.jsx`; `docs/reports/report_task_image_merge_unify_1A.md` |
| 27 | Мікс photo+PDF у DP image-merge: TASK 1B обмежив scope чистими все-image вхідними наборами. При змішаному батчі (наприклад 3 фото + 1 PDF) DP показує акуратний `toast.warning` «оберіть або тільки фото, або тільки PDF» і вимагає від адвоката розділити пакет. Інтерактивна склейка фото у DP працює лише для чистих наборів фото. | Реалізація mix-режиму = два сценарії в одному UI (image-merge editor + класична нарізка PDF одночасно), що ламає принцип «одна відповідальність на вікно». Адвокат і так зазвичай має чистий потік: або фото з телефону, або PDF-том із суду. Якщо реально потрібно — два запуски DP. Toast чіткий, не крах, не блокує іншу роботу. | Якщо реальні скарги адвоката на mix-режим >2 разів → окремий TASK «DP image-merge mix mode»: розділити вхідний батч на photo-subset (→ image-merge editor) і PDF-subset (→ класичний pipeline.run), отримати два результати в одній сесії. Або UI-обмеження «закидайте окремо» лишити як свідому межу UX. | TASK 1B image_merge_unify (2026-05-29); `src/components/DocumentProcessorV2/index.jsx startProcessing`; `docs/reports/report_task_image_merge_unify_1B.md` |
| 28 | DP image-merge editor: cross-group drag працює тільки на drop НА фото іншої групи (over.id парситься з ID цільового item). Drop у порожню область неіснуючої групи (між групами / у new empty group) повертає `null` декодуванням → drag ignored. Адвокат повинен попадати у конкретний thumbnail цільової групи; працює, але потребує точності. Empty groups (через «Додати порожню групу») приймають drop тільки якщо contains placeholder thumbnail. | Multi-container @dnd-kit з реальним drop-on-container droppable area = окрема велика інтеграція (потребує useDroppable per group container + DragOverlay для preview + onDragOver/onDragEnd reconciliation для over-empty випадків). Поза scope MVP 1B (адвокат-диригент для image-merge — спрацював, далі правка UX окремо). | **✅ ЗРОБЛЕНО 2026-05-31 (Lane B, B2, у main).** Реалізовано `useDroppable` per `GroupSection` (контейнер `g::<docId>::container`), кастомний `collisionDetection`, `DragOverlay` для прев'ю, drop-on-empty/over-container — drop у порожню/нову групу тепер працює. | TASK 1B image_merge_unify (2026-05-29) — відкрито; закрито Lane B B2 (2026-05-31); `src/components/DocumentProcessorV2/DpImageMergeEditor.jsx`; `docs/reports/report_task_image_merge_unify_1B.md`; `docs/reports/report_laneB_ui_debts.md` |
| 29 | Інкрементальне оновлення `case_context.md`: TASK 2 робить ПОВНУ регенерацію нарису справи з усього набору документів (DP-тригер і кнопка «Огляд» однаково проганяють усі документи через OCR + один AI-виклик). На товстих справах (десятки документів) це дорого/повільно — щоразу повний OCR-batch + 16K-токен генерація. | Повна регенерація — простий, передбачуваний, behavior-preserving крок (покриває обидва кейси «немає файлу → створює» і «є → перезаписує свіжим»). Інкремент має реальний ризик: деградація звʼязності summary при дописуванні + новий документ може змінити сенс старих розділів. YAGNI поки справи невеликі. | Коли повна регенерація стане дорогою/повільною (реальна скарга адвоката на час або вартість на товстій справі) → окремий TASK «інкрементальний case_context»: брати готовий нарис + лише нові документи DP-сесії, органічно дописувати; можливий гібрид (інкремент + періодична повна); можливо окрема кнопка (не обовʼязково в DP). | TASK 2 context_generator_unify (2026-05-30); `src/components/CaseDossier/services/contextGenerator.js`; `docs/reports/report_task_context_generator_unify.md` |
| 30 | **Уніфікація UI-рендеру зображень/документів — single source.** Рендер картинок з трансформаціями (auto-orientation + crop запечені у blob, user-rotation через CSS) має йти через ОДИН спільний шлях (`components/ImageEditor/`, `services/sortation/imageRenderer.computeRenderedBlob`). Crop-баг у DP (2026-05-29) стався саме тому, що `DpImageMergeEditor` не використав спільний рендер, а продублював логіку (`previewUrls=null`). Ширше: накопичений UI-борг у досьє (великі файли, CSS-префікс #26, дубльована логіка рендеру) росте з кожною новою UI-фічею. | Окремий великий «TASK прибрати UI-борг» гниє без видимої користі (низький пріоритет — класична пастка, refactoring roadmap §16.2). Натомість борг платиться **інкрементально**: кожен UI-таск тягне зі спільного компонента, не дублює (нове правило-сторож у `DEVELOPMENT_PHILOSOPHY.md` «Спільний рендер UI»). | **НАСКРІЗНА ВИМОГА (активна з 2026-05-30):** кожен TASK що рендерить зображення/документи — через спільний `ImageEditor`/`imageRenderer`, без локальних дублів (Rule of Three → винос). Структурний спліт `CaseDossier/index.jsx` (3260 рядків) — підняти в чергу ПІСЛЯ TASK 4 першого фронту (refactoring roadmap Фаза 4), не відкладати в кінець. | TASK 1B crop-fix (2026-05-29); `docs/reports/report_task_image_merge_unify_1B.md`; `consultation_large_files_refactoring_roadmap.md`; `DEVELOPMENT_PHILOSOPHY.md` (правило «Спільний рендер UI») |
| 31 | **Уніфікований стан «вичерпано баланс / ліміт токенів» наскрізно по всіх AI-точках.** Зараз помилка Anthropic `API 400 invalid_request_error "Your credit balance is too low…"` (і майбутній SaaS per-user ліміт токенів) падає в загальні catch-и кожної AI-точки і показується як технічна помилка / маскується. Реальний кейс (2026-05-31): нульовий баланс → «Огляд» показував «Перевірте API ключ» (хибно), DP-шлях падав тихо. Після SaaS-комерціалізації КОЖЕН користувач матиме ліміт; вичерпання має бути окремим **розпізнаваним станом** з чітким повідомленням («Закінчився ліміт/баланс — поповніть», не «щось зламалось»). Стосується ВСІХ ~10 точок Anthropic API (App.jsx ×3, Dashboard, CaseDossier чат-агент, contextGenerator, claudeVision, imageSortingAgent, imageDocumentGrouper, toolUseRunner) + усіх відповідей агентів і тостів. | Зараз єдиний споживач — founder з власним ключем; розмаскування помилки (TASK 2026-05-31) уже показує реальний текст, тимчасово достатньо. Централізований детектор «balance/limit exhausted» + єдиний UX-стан = крос-модульна зміна всіх AI-точок, передчасна поки немає multi-user і per-user лімітів (та сама пастка що «потім додамо» — але тут свідомо чекаємо на реальну потребу SaaS, не забуваємо). | **TASK Multi-user Activation АБО SaaS-комерціалізація** (коли `subscription.limits` стають не-null і вичерпуються реальними користувачами): ввести спільний хелпер (напр. `isQuotaExhaustedError(apiResponse)` у `aiUsageService`/`subscriptionService`) що розпізнає і Anthropic `credit balance too low`, і внутрішній per-user token-limit; усі AI-точки маплять його в єдиний UX-стан «ліміт вичерпано» (окремий тост-variant + повідомлення агента), з посиланням на поповнення/тариф. Звʼязати з `subscriptionService.checkLimits` (blockAt:100). | Діагностика контексту (2026-05-31); розмаскування у `CaseDossier/index.jsx handleCreateContext` catch + DP-тригер; CLAUDE.md §SaaS `subscription.{limits\|current\|alerts}`; `subscriptionService.js` |
| 32 | Розбіжність ліміту LIFO-ротації `ai_usage[]`: канонічний `MAX_AI_USAGE_ENTRIES = 50000` (`aiUsageService.js:19`, застосовується у `logAiUsage`/`logAiUsageViaSink`), але паралельний sink у `App.jsx:3733` ріже на `100000` (`next.slice(next.length - 100000)`). Один масив — два різні пороги обрізки залежно від точки запису (#11: одне поле = один сенс). Наслідок: фактичний розмір `ai_usage[]` недетермінований (до 100k якщо останній запис ішов через App-sink, до 50k через сервіс), впливає на повноту історичних даних для аналізу витрат. | Латентне ще до поточних TASK; жоден тест не падає (обидва — валідні числа, лише різні). Виправлення зараз = behavior-зміна поза scope діагностики контексту. CLAUDE.md документує «LIFO 50000» — App.jsx розходиться з документацією. | TASK AI Cost Analysis (`docs/tasks/TASK_ai_cost_analysis_and_forecast.md`) виявить вплив на дані АБО наступне редагування `App.jsx` ai_usage-sink / `aiUsageService` по суті → звести на єдину константу `MAX_AI_USAGE_ENTRIES` (імпортувати в App.jsx замість літерала 100000), узгодити з CLAUDE.md. | Діагностика контексту / підготовка TASK AI Cost Analysis (2026-05-31); `src/services/aiUsageService.js:19,65`; `src/App.jsx:3733`; CLAUDE.md §СТРУКТУРА ДАНИХ «ai_usage[] LIFO 50000» |
| 33 | **Решта дубльованої логіки image-editor (модалка ↔ DP) після виносу групування.** Аудит 2026-05-31 (по завершенні виносу `grid/displayItems.js`) знайшов ще шматки, реалізовані ДВОМА копіями: `previewUrls` async-генерація (~70 рядків, `DpImageMergeEditor.jsx:232-299` ≈ `ImageMergePanel/index.jsx:174-249` — найбільший), `cropStateByIndex` (`PreviewView:87`=`DP:610`), `countActiveCrop` (`PreviewView:106`=`DP:627`), `flatPositions` (`SortableGrid:66`=`DP:828`), `countActiveDuplicateGroups`/`uncertainSet` (банер). Не баги — функціонально однакові; ризик дрейфу (зміна в одному → забута в близнюку), той самий клас що дав баг групування. Конкретизація наскрізної вимоги #30. | DP щойно стабілізовано після кількох невдалих сесій — повторне відкриття делікатне. Це борг, не баг: функціонально все працює. За політикою #30 борг платиться **інкрементально**, не окремим «великим cleanup» що гниє. Робити зараз = відкривати DP заради косметики перед функціональною роботою. | **✅ ЗРОБЛЕНО 2026-05-31 (Lane B, B1 фази 1+2, у main).** Винесено у спільне: `buildFlatPositions`, `countActiveDuplicateGroups` (`grid/displayItems.js`); `buildCropStateByIndex`, `countActiveCrop`, `buildUncertainSet` (новий `services/imageDocument/cropState.js`); найбільший дубль `previewUrls` (~70 рядків ×2) → хук `hooks/usePreviewUrls.js`. Обидва споживачі імпортують, інлайн-копії видалено. Парний #30 (повна уніфікація рендеру) — лишається відкритим (ширший намір). | Аудит спільного image-editor (2026-05-31) — відкрито; закрито Lane B B1 (2026-05-31); таск-спека `docs/tasks/TASK_imageeditor_share_remaining_helpers.md`; `docs/reports/report_laneB_ui_debts.md`; парний борг #30 (лишається) і #28 (закрито B2) |
| 34 | **Видима індикація прогресу для довгих async-операцій — контекст-генератор + обробка фото.** Обидві можуть тривати довго (контекст: OCR-batch + 16K-ген на товстій справі; фото: підготовка/орієнтація/OCR/sort десятка HEIC = хвилини), і обидві зараз ідуть **мовчки**: інтерфейс ніби зависає, адвокат не розуміє «працює чи зламалось», сигнал лише наприкінці тостами. Контекст: DP-шлях `onProgress: () => {}` (без UI), кнопковий шлях пише `contextMsg` рядком (не помітно). Фото: `DpImageMergeEditor` startup (`prepareImagesForMerge` + per-group `sortImageDocument`) — без візуального прогресу. Потрібен єдиний патерн індикації (поп-ап АБО фоновий бейдж в інтерфейсі), що тримається доки операція не завершиться. | UX-покращення, не блокер: операції відпрацьовують коректно (підтверджено на порожній справі + на фото), результат правильний. Робити «на ходу» в делікатному DP перед стабілізацією решти — зайвий ризик; до того ж це **спільний** UX-компонент (один індикатор для обох точок) — заслуговує власного маленького TASK, а не вплітання в інші зміни. | **ОНОВЛЕННЯ 2026-05-31:** B3 (Lane B, у main) зробив **спільний компонент** `ProcessingProgress` і підключив його у модалці (`ProcessingView`) + бейдж аналізу країв у `DpImageMergeEditor` — тобто інструмент готовий. **РОЗДІЛЕНО на дві половини:** (а) **фото-частина** — **✅ ЗРОБЛЕНО 2026-05-31 (у main, `dd3a121`):** важка обробка `prepareImagesForMerge`(heic/ocr/rotate)/grouper/`sortImageDocument` у `index.jsx` тепер показує повноекранний оверлей зі спільним `ProcessingProgress` (локальний `mergeProgress`, очищення в `finally`, job-систему не чіпали; тест `dp-image-merge-progress`); (б) **контекст-частина** (`generateCaseContext`) — **ЛИШАЄТЬСЯ ВІДКРИТОЮ**, окремий крок. **Тригер контекст-частини:** окрема маленька задача або наступний дотик до UX контекст-генератора по суті — підключити вже наявний `onProgress`/прогрес-колбек `generateCaseContext` до того самого спільного `ProcessingProgress` (правило #30, не дублювати). | Обговорення 2026-05-31 (по сліду виносу контекст-генератора і фіксу обробки фото); `src/components/CaseDossier/services/contextGenerator.js` (`onProgress`); `src/components/ImageEditor/ProcessingProgress.jsx` (готовий спільний компонент); `src/components/DocumentProcessorV2/index.jsx` (`onProgress` ≈271, console.log-заглушка); парний борг #30 |
| 35 | Глибший корінь втрати `ai_usage[]` (за shrink-guard'ом TASK AI Cost Analysis): сесія записала на Drive порожній/майже-порожній in-memory `ai_usage` поверх повної історії. Save-ефект уже має gate `if (!driveHydrated) return` (`App.jsx`), отже формально hydration завершився — але стан `aiUsage` на момент запису був порожнім (Drive-`ai_usage` уже був скинутий ранішою сесією, АБО `registry.ai_usage` не був масивом і `setAiUsage` на load пропустився, лишивши localStorage-seed `[]`). Точний тригер не відтворюється статичним аналізом — потрібен runtime-лог послідовності load→save. Shrink-guard (`registryWriteGuard.js`) — сітка безпеки, що ловить наслідок; першопричину (запис із недогідрейченого/застарілого стану) не усуває. | Guard уже зупиняє втрату даних (достатньо для негайної безпеки, PRINCIPLE DELTA). Глибша діагностика hydration-послідовності потребує runtime-телеметрії, якої зараз нема; робити «наосліп» рефактор load/save-ефектів — ризик регресій у критичному Drive-sync. Поза scope аналітичного TASK. | Якщо guard почне спрацьовувати в реальній роботі (`guard_blocked` з `ai_usage_*` у console/`driveSyncStatus='error'`) АБО наступний TASK торкається hydration/persist-ефектів `App.jsx` (EFFECT-A/EFFECT-B) → додати runtime-лог послідовності `setAiUsage`-on-load vs save, переконатись що load завжди заповнює `ai_usage` перед першим save, розглянути gate саме на `ai_usage`-hydrated, а не лише `driveHydrated`. | TASK AI Cost Analysis (2026-05-31); `src/App.jsx` EFFECT-A (`setAiUsage` на load ~4246), EFFECT-B (save gate `!driveHydrated`); `src/services/registryWriteGuard.js`; `docs/reports/report_ai_cost_analysis_2026-05.md` |
| 36 | **DP image-merge: вільне перетягування фото між групами (вкл. у порожні/нові) + допилити «Додати порожню групу».** Продуктовий драйвер: набір фото = кілька документів, а згруповано в один (або навпаки) → адвокат має додати групу і перетягнути відповідні аркуші, щоб розділити на N документів. Зараз `addEmptyGroup` (`DpImageMergeEditor.jsx:521`) дозволяє додати **скільки завгодно** груп (обмеження немає — здалось хибно), АЛЕ **порожня група не є валідною drop-ціллю**: drop спрацьовує тільки НА фото іншої групи (`handleDragEnd:350`), не на контейнер/порожню зону — тому додана порожня група **марна** (фото в неї не затягнути). Технічний механізм — рівно борг #28 (drop-on-container). Опційно: зовнішній drag-drop фото ззовні у групу (адвокат сумнівається чи доречно — OCR/Document AI вже відпрацювали; ймовірно поза обсягом, лишити drag лише в межах редактора). | Не баг — додавання/перетягування-на-фото працює; це **незавершена UX-фіча**. Робилось як MVP 1B «адвокат-диригент»; повноцінний multi-container drop = окрема DnD-інтеграція (useDroppable per group + DragOverlay + onDragOver reconciliation), свідомо відкладена від MVP. Тепер береться в роботу (консолідація UI-боргів 2026-05-31). | **✅ ЗРОБЛЕНО 2026-05-31 (Lane B, TASK B2, у main).** Закрито борг #28: `useDroppable` per `GroupSection` (контейнер `g::<docId>::container`), кастомний `collisionDetection`, `DragOverlay` для прев'ю, drop-on-empty/over-container; add-group необмежений; порожня група — валідна drop-ціль; lifecycle (авто-видалення лише спорожнілої source-групи). Перевірено координатором + адвокатом (drag працює). | Обговорення 2026-05-31; `src/components/DocumentProcessorV2/DpImageMergeEditor.jsx` (`handleDragEnd`, `collisionDetection`, `GroupContainerId`); борг #28 закрито разом; `docs/reports/report_laneB_ui_debts.md` |
| 37 | **DP image-merge: кнопка «Впорядкувати сторінки» в межах документа (later — команда агента досьє через tool use).** Після того як grouper розбив фото на документи і адвокат вручну перетягнув аркуші у відповідні слоти (борг #36), порядок сторінок усередині документа лишається довільним. Потрібна кнопка на кожному документі-групі, що перезапускає AI-сортування сторінок поточної групи і застосовує новий порядок прямо у вікні редактора. Будівельний блок **уже є** — `sortImageDocument` (повертає `{order, duplicates, suggestedName, ...}`, уже викликається при первинній обробці per-group). | Не баг — первинне сортування працює; це нова **on-demand** фіча. Вплітати в активну Lane B-роботу/прогрес — змішування намірів (правило #11). Tool-use для агента досьє ще не закладено (закладається вперше в DP v2). Заслуговує власної маленької задачі. | **Окрема маленька задача** (або при міграції агента досьє на tool use — DP v2): кнопка «Впорядкувати сторінки» per `GroupSection` у `DpImageMergeEditor`, що мапить поточні фото групи у shape `sortImageDocument` і застосовує `order`; логіку винести так, щоб **кнопка І майбутня команда агента досьє кликали ОДИН код** (Rule of Three / #11). Білінг — через наявне C7-логування у `sortImageDocument`. | Обговорення 2026-05-31 (по сліду боргу #36 — ручне перетягування між групами); `src/services/imageDocument/sortImageDocument.js`; `src/components/DocumentProcessorV2/DpImageMergeEditor.jsx`; CLAUDE.md § «TOOL USE СТРАТЕГІЯ» |
| 38 | **Уніфікація прогрес-індикації довгих DP-операцій + готовність до «комбо» (агрегатор сегментів).** ЗАРАЗ паралельно існують ДВІ системи прогресу: (1) **job-based** (стадійна) — `GlobalProgressScreen`/`ProgressFullScreen`/`JobProgressTopbar` через `jobProgressStore` (`DocumentPipelineContext` + `streamingExecutor` + `stageLabels`), обслуговує **нарізку**/основний DP-пайплайн (адвокат повідомив про баги/невдалий UX 2026-05-31); (2) **component-based** (фазна) — спільний `ProcessingProgress` (`ImageEditor/`), обслуговує **фото** (модалка `ProcessingView` + DP image-merge overlay, докручується Lane B follow-up сесією). Image-merge свідомо обходить job-store (стадійна модель погано лягає на heic/ocr/rotate/grouper/sort). Майбутній режим **«комбо»** (фото + нарізка + розпак zip + розкладка в папки + ін., запускані в комбінації) потребує ОДНОГО бару, що **нанизує/сумує сегменти** кожної під-операції. | Будувати агрегатор зараз — YAGNI (комбо ще не TASK; speculative generality). Дві системи поки виправдані: різні моделі (стадії vs фази), кожна працює у своєму сценарії; зводити «про запас» = чистити те, чого поточний TASK не активує (золота середина). Шов уже закладається: фото-прогрес віддає чистий сегмент `{phase, done, total}` через спільний `ProcessingProgress` — придатний до агрегації пізніше («ембріон з повним ДНК»). | **Коли «комбо» стане реальним TASK у DP** (кілька під-операцій в одному запуску) → побудувати ОДИН progress-агрегатор: кожна під-операція = сегмент `{phase, done, total}`, агрегатор сумує/секвенує, один спільний `ProcessingProgress` рендерить; фото стає сегментом (не окремий бар); свідомо обрати канонічну систему (ймовірно job-based як природний агрегатор АБО component-based) і звести другу до неї (правило #11/#30 — одна модель прогресу); заразом виправити баги стадійного бару нарізки. | Обговорення 2026-05-31 (дизайн прогрес-барів, по сліду фото-прогресу Lane B follow-up); job-система: `src/components/DocumentProcessorV2/{GlobalProgressScreen,ProgressFullScreen,useJobProgress}`, `src/contexts/DocumentPipelineContext.jsx`, `src/services/documentPipeline/{streamingExecutor,stageLabels}.js`; component-система: `src/components/ImageEditor/ProcessingProgress.jsx`; парний борг #30 (наскрізна уніфікація UI/рендеру) |
| 39 | **Локальний ре-компресор сканованих PDF (рендер→downscale→пересобрати), без iLovePDF/зовнішнього API.** Адвокат уточнив (2026-05-31): **>80%** документів — це вже-готові скановані PDF (телефонний «Сканувати документ» на iPhone/Android пакує кадри в PDF; клієнти шлють сфотографоване як PDF), і вони **великі за об'ємом**. Склейка окремих фото (image-merge) — навпаки нішева фішечка. Тобто зменшення розміру вхідних PDF — основна потреба, не корнер-кейс. Прагматичний шлях НЕ парсити нутрощі PDF (там пекло — загальний компресор = місяці/роки): `pdf.js` рендерить кожну сторінку в canvas цільового розміру → `downscaleImage` → `jsPDF`/`pdf-lib` пересобирає. Цеглини вже в стеку → тижні, не роки. | Зараз першим іде downscale фото image-merge (`TASK_image_downscale_blob_hygiene.md`) — він закриває пам'ять/лаг і дає примітив `downscaleImage`, який цей TASK перевикористає. Вплітати ре-компресор у нього = змішування намірів (image-merge ≠ вхідний PDF). Загальний PDF-компресор (будь-який PDF без втрат) свідомо НЕ робимо — непосильно; беремо лише вузький сканований кейс. | **Після того як `downscaleImage` приземлиться (TASK downscale фото)** АБО коли розмір вхідних сканованих PDF стане активним болем / рішення прибрати залежність від iLovePDF → окремий focused TASK: `pdf.js` render-per-page у цільовому масштабі → `downscaleImage` → `jsPDF`/`pdf-lib` rebuild. **ГЕЙТ лише на скановані** (`documentNature='scanned'`) — цифровий текстовий PDF НЕ растеризувати (втратиться текстовий шар + він і так малий). Драйвери: розмір, вартість (без API), менша залежність, і головне — **конфіденційність** (клієнтські документи не йдуть на чужий сервер — адвокатська таємниця). | Обговорення 2026-05-31 (стиснення сканованих PDF локально замість iLovePDF); перевикористовує `downscaleImage` з `docs/tasks/TASK_image_downscale_blob_hygiene.md`; цеглини в стеку: `pdf.js` (`ocr/pdfjsLocal.js`), `pdf-lib`, `jsPDF` (`converter/imageToPdf.js`); споріднений з clean-text-гейтом `documentNature='scanned'` |
| 40 | **Downscale не дістає Drive-джерельних фото — виграш пам'яті/розміру лише для завантажених з пристрою.** Крок 1.5 у `prepareImagesForMerge` пропускає файли з `_isDriveSource` (downscale не застосовується). Тобто важкі фото, витягнуті з Google Drive, у браузера не легшають; виграш — лише для device-upload. | НЕ дефект, а коректна межа: `ocrOneImage` для Drive-джерельних читає файл **з Drive за id** (`{id: file._driveId}`), не локальний blob. Downscale+upright локального blob розсинхронізував би orientation-сигнал кроку 3 (він бере `ocrResults` з Drive-оригіналу) → подвійний/хибний поворот. Розплутати = змусити OCR читати локальний зменшений blob (а не Drive-оригінал) АБО downscale+перезалив на Drive — більший скоуп, заплутаний з оптимізацією `_isDriveSource` (пропуск re-upload, бо driveId уже відомий). Поза скоупом Коміту 1. | Якщо важкі Drive-джерельні фото лишаться реальним болем пам'яті в роботі → розширити downscale на Drive-source: або OCR читає локальний зменшений upright-blob (узгоджена орієнтація), або downscale+перезалив; розв'язати зчеплення orientation-сигналу з джерелом OCR. **ОНОВЛЕННЯ 2026-05-31: адвокат підтвердив — Drive-source є ДОМІНАНТНИМ шляхом інгесту фото (телефон → Drive-папка → планшет/десктоп), не край.** Тобто межа б'є по головному маршруту. Але клієнтський ретрофіт сплетений зі сховищем (Drive-файл = збережений оригінал за `driveId`). **Стратегічний фікс — не ретрофіт, а downscale на ВХОДІ через Telegram-бот/сервер** (див. #41 і `docs/consultations/ideas_photo_ingestion_workflow.md`): великий Drive-оригінал просто не виникає. | TASK image_downscale_blob_hygiene Коміт 1 (2026-05-31); `src/services/imageDocument/prepareImagesForMerge.js` (крок 1.5 skip `_isDriveSource`, `ocrOneImage` Drive-read за id); `src/components/CaseDossier/{AddDocumentModal,ImageMergePanel/index}.jsx` (ставлять `_isDriveSource`); `docs/consultations/ideas_photo_ingestion_workflow.md`; парний #41 |
| 41 | **Telegram-бот інгест фото (закидати фото боту → автоматично у справу).** Адвокат (2026-05-31): більшість фото походять з телефона (сам знімає / клієнти шлють у Viber/Telegram/email/розшарені Drive-папки); BMS у браузері телефона незручний, тому маршрут «телефон → Drive-папка → планшет/десктоп». Telegram-бот зняв би всю цю морду: фото боту → одразу у справу. **Обов'язкова функція (слова адвоката).** Архітектурний бонус: бот зменшує фото **при отриманні** (серверно) → закриває межу #40 (великий Drive-оригінал не виникає) у корені. | Велика інтеграція (бот + бекенд/інгест-pipeline + прив'язка до tenant/справи) — поза поточними дрібними TASK'ами; потребує серверної частини (зараз застосунок суто клієнтський). Будувати зараз = передчасно (немає бекенда; SaaS-комерціалізація ще попереду). | Коли візьметься **TASK Telegram бот** (уже в роадмапі CLAUDE.md «Заплановано») АБО коли інгест фото з телефона стане пріоритетним болем → реалізувати інгест: фото боту → downscale на вході → `createDocument`/pipeline у потрібну справу; узгодити з tenant-моделлю і білінгом. | Обговорення воркфлоу 2026-05-31; `docs/consultations/ideas_photo_ingestion_workflow.md`; CLAUDE.md «Заплановано → TASK Telegram бот»; парний #40 (downscale Drive-source) — Telegram закриває його в корені |
| 47 | **Редагований почищений документ + менеджер міток уваги (per-mark, ручне редагування, через агента).** TASK 3.2 дає показ/сховати підсвітки, навігацію і «зняти всі назавжди». Наступний крок: на кожній мітці у списку — дії ✎ поправити / ✕ прибрати цю окремо; ручне редагування тексту почищеного `.md` у в'ювері; те саме через **агента досьє** (ACTIONs на правку/зняття міток). | Це повноцінна фіча «редагований `.md`» (editable viewer + persist + ACTIONs агента), не деталь 3.2 — роздула б фазу. Поточний дизайн (мітки `==…==` у `.md`, список за порядком, чип/панель) **навмисно розширюваний**: дрощується дії на пункт без рефактору. | Окремий TASK після 3.2 → редагований почищений `.md` у в'ювері (per-mark ✎/✕, ручна правка) + дзеркало через агента досьє. Логічно разом із #43 (layout-as-source) і посторінковим показом (5.2) — усі про показ/редагування `.md`. | Обговорення 2026-06-01 (clean_text UX міток); `docs/tasks/TASK_3.4_attention_highlights.md`; `MarkdownRenderer`; парні #43, micro-task 5.2 |
| 45 | **Пакетна (Batches API) обробка для несрочної масової очистки — потенційна −50% вартості.** Batches API дає −50% ціни на несрочні запити (результат до 24 год). DP-авто-пост-крок очистки (фон) і сценарій «почистити всю справу на ніч» — природні кандидати. Інтерактивні кнопки (адвокат чекає) — ні, тільки Standard API. | Async-модель (submit → poll → результат пізніше) у суто клієнтському застосунку незручна — немає кому забрати відкладений результат якщо вкладку закрито; потребує серверної частини. Зчеплено з #41 (Telegram-бот / бекенд). | Коли зʼявиться серверна частина (Telegram-бот / бекенд) АБО масова очистка стане вартісним болем → провести фонову очистку через Batches API (−50%). | Обговорення 2026-06-01 (clean_text вартість); парний #41 (Telegram/бекенд); docs.anthropic.com (batch −50%, prompt caching −90%) |
| 44 | **`MODEL_PRICING` застарів — оцінки вартості у білінг-телеметрії неточні.** `aiUsageService.MODEL_PRICING` датовано 2026-05-04 (CLAUDE.md помітка «verify quarterly»). Актуальні опубліковані: Haiku 4.5 **$1/$5** (було $0.80/$4); Opus **4.8 $5/$25** (було 4.7 $15/$75 — різко дешевше); Sonnet ~$3/$15. `estimatedCostUSD` у `ai_usage[]` зараз рахується по застарілих цифрах. | Дрібний фікс, не блокує роботу; поза скоупом 3.1. Не критично поки білінг-UI немає (оцінки лише для оператора SaaS). | Окремий дрібний TASK: оновити `MODEL_PRICING` + (можливо) `claude-opus-4-7` → `claude-opus-4-8` у `modelResolver`; звірити з docs.anthropic.com/pricing; додати дату звірки. | Перевірка цін 2026-06-01; `aiUsageService.js MODEL_PRICING`; CLAUDE.md «MODEL_PRICING … verify quarterly» |
| 43 | **Уніфікувати джерело тексту: для `scanned` тримати `.layout` замість `.txt`; viewer рендерить текст з layout посторінково (як у Word).** `.layout.json` містить повний текст (`page._text` на сторінку) + геометрію — це **надмножина** `.txt`. Зараз для сканів тримаємо `.txt` + `.layout` (+ `.md` після очистки). Ідея: лишити **`.layout`** (сире джерело — для повторної очистки і сирого показу) + **`.md`** (чистий вигляд), `.txt` прибрати; в'ювер витягує `page._text` і показує посторінково. | (1) `.layout` важчий (~100-500КБ проти КБ `.txt`) — більше Drive-обсягу; (2) `searchable` (DOCX/HTML/текстовий PDF) layout **не мають** — їм потрібен `.txt` → змішана модель scanned/searchable; (3) `.txt` читає не лише viewer а й `contextGenerator`/інші через `ocrService.getCachedText` → ширший рефактор шару споживачів тексту, поза скоупом 3.1. | Окремий TASK «стратегія артефактів тексту» АБО разом із відкладеним посторінковим показом у viewer (micro-task 5.2 — «окремі аркуші всередині документа»): природно одна задача. Рішення доль артефактів у 3.1 (success: `.txt`→`_raw_txt`, видалити `.layout`) — **тимчасове** до цього TASK. | Обговорення 2026-06-01 (clean_text, доля артефактів); `cleanTextService.js` (layout shape `page._text`); `ocrService.getCachedText`/`serializeLayout`; `docs/reports/report_micro_task_5_2_fix2.md:297` (окремі аркуші — відкладено) |
| 42 | **Тривалі процеси DP + їхній UI/результат не переживають навігацію (модуль розмонтовується).** ROOT (звірено): App рендерить модулі умовно `{tab==='X' && <Module/>}` (`App.jsx:5178+`) → перемикання вкладки / вхід у досьє РОЗМОНТовує модуль. Увесь стан `DocumentProcessorV2` — `selected` (завантажені фото), `imageMerge` (екран редагування), `mergeProgress`, `result` — локальний (`useState`, `index.jsx:89-121`) → при навігації знищується. Image-merge ще й обходить job-store (повністю ефемерний). Нарізка (job-based) частково виживає (job у `DocumentPipelineProvider` + `GlobalProgressScreen`/`JobProgressTopbar` змонтовані ВИЩЕ вкладок, `App.jsx:5127/5382`), АЛЕ показ `result` локальний → теж губиться. Три facet'и адвоката: **(a)** процес має ДОПРАЦЮВАТИ у фоні при переході на іншу вкладку/дашборд + **тост по завершенні** (не авто-перемикання — дратує), «зайдіть у DP подивитись»; **(b)** in-progress UI (завантажені фото / екран редагування / поп-ап прогресу) не має зникати ні від тапа по вільній зоні (модалка), ні від перемикання вкладки — лишатись доки адвокат не скасує/не запустить; **(c)** результат останньої сесії має зберігатись при поверненні (зараз тримається лише поки ти на тій вкладці) + історія **останніх ~5 сесій** (нарізка/склейка) з можливістю переглянути. | Велика архітектурна зміна: підняти виконання + стан + результати у персистентний шар НАД навігацією (store/context рівня App, як наявний `jobProgressStore`, але поширити на image-merge + тримати результати + історію N), щоб UI став VIEW, який можна розмонтувати без зупинки процесу/втрати результату. «На ходу» зараз — зайвий ризик у делікатному DP; тісно зчеплено з #38 (уніфікація двох систем прогресу — той самий персистентний шар). | Коли візьметься #38 (уніфікація прогресу) АБО окремий TASK «персистентність процесів DP» → винести стан image-merge/нарізки у persistent store над вкладками; результати + історія останніх N сесій (у store, можливо дубль на Drive); тост по завершенні без авто-switch; guard модалки від dismiss при load/in-progress. **Перевірити рантайм:** чи фоновий процес реально триває після unmount (промис висить, setState у мертвий компонент → втрата); поведінку backdrop-кліку модалки. | Обговорення 2026-05-31 (персистентність процесів/результатів); ROOT звірено: `src/App.jsx:5178+` (умовний рендер вкладок), `:5127/5382` (Provider/GlobalProgressScreen над вкладками), `src/components/DocumentProcessorV2/index.jsx:89-121` (локальний стан); парний #38 (уніфікація прогресу) і #34 (контекст-прогрес) |
| 46 | **Винос геометрії layout у спільний `layoutGeometry` (blockBox/orderedBlocks/footer).** `cleanTextService.js` тримає локальну копію-дзеркало цих хелперів з `pageMarkers.js` (там вони module-private). Два місця з геометрія-математикою = мʼяка напруга #11. | Виносити зараз = редагувати `pageMarkers.js` (робочий DP-пайплайн); копія мала і чиста; третього споживача ще нема (Rule of Three не спрацював). | Коли зʼявиться третій споживач geometry-хелперів АБО при загальному рефакторі DP-пайплайна → винести у спільний `src/services/layoutGeometry.js`, `pageMarkers.js` + `cleanTextService.js` імпортують звідти. | TASK 3.1 (2026-06-01); `src/services/cleanTextService.js` (локальні `blockBox`/`orderedBlocks`); `src/services/documentPipeline/pageMarkers.js` |
| 48 | **Sliding window / multi-window Triage для дуже великих томів — як СЕРВЕРНА фіча.** Різник обробляє том одним проходом (компактний паспорт). Альтернатива для томів понад те, що тягне однопрохідний паспорт: різати на вікна, кожне через різник окремо, зшивати план; документ на стику переноситься «відкритим хвостом» + нахлест. Цінність: запас за межі ~250-300 стор. + точність на великих томах (коротший контекст = менше дрейфу). | (1) Потреба для норми 200-250 стор. **уже закрита** однопрохідним компактним паспортом (`TASK_smart_triage_passport_scale_and_text`) — sliding window для ≤250 не потрібен. (2) Уся складність і ризик серйозних багів — у **зшиванні через межу вікна** (загублена/не туди підшита сторінка на юр. документах). (3) На клієнті паралелізм крихкий, ліміти RAM/латентності; запобіжники для стиків (окремий прохід реконсиляції + дедуп нахлесту) доступні **тільки серверно**. Інфраструктура патерну вже є (`multiFileReconstructor openTails` для стиків, `resolveOverlaps` для дедупу) — адаптувати «файл»→«вікно». | **Серверний перехід DP** (бекенд/воркер) → будувати sliding window серверно-нативно: вікна, нахлест, окремий прохід реконсиляції стиків + дедуп, тест-матриця (документ у 1 вікні / перетин 2-3 вікон / дедуп / розрив). До того — однопрохідний паспорт лишається шляхом за замовчуванням. | Консолідація 2026-06-02 — `docs/consultations/consultation_sliding_window_triage_server_feature.md`; джерела: `consultation_dp_product_vision.md` §6.2/Фаза4, `consultation_combined_roadmap_dp_and_refactoring.md` Фаза6, `diagnostic_dpv2_call_trace_and_chaos.md` (`_windows/<N>.json`) |

---

Поповнюється з кожним TASK (аудити, cleanup'и, виноси). Закритий запис —
видаляється з таблиці після того, як тригер спрацював і борг сплачено
(слід лишається в git-історії та у відповідному `report_*.md`).
