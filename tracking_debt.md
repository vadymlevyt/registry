# tracking_debt.md — Реєстр свідомо відкладеного

Живий реєстр рішень «свідомо НЕ робимо зараз». Механізм золотої середини
(DEVELOPMENT_PHILOSOPHY.md, розділ «ПРИНЦИП ЗДОРОВОГО ОРГАНІЗМУ»): чистимо/будуємо
тільки те, що активується поточним TASK; решта — сюди, з **явним тригером**.

**Правило входу:** запис без конкретного тригера (подія, не «колись») — невалідний.
Це не борг, а забування. Тригер має бути перевіряємим.

| # | Що відкладено | Чому свідомо не робимо зараз | Явний тригер активації | Джерело / посилання |
|---|---------------|------------------------------|------------------------|---------------------|
| 1 | Backfill denormalized `case.client` (string) і `proceeding.judges` (string) із `parties[]` / `composition` | v7 ввів канонічні `parties[]`/`composition`; UI ще читає старі summary-поля. Переписувати UI зараз — поза обсягом інфраструктурного v7. | Коли реальний канал (Court Sync) почне писати `parties[]`/`composition` і UI треба показувати дані з них → окремий backfill TASK (читає parties → генерує summary, UI не змінюється до того). | ARCHITECTURE_HISTORY.md § «Canonical Schema v7 → Tracking debt»; `report_task_0_3_5_canonical_schema_v7.md` |
| 2 | Перевірка повноти консолідованого дайджесту після виносу історії | Винос CLAUDE.md → ARCHITECTURE_HISTORY.md зробив дайджест ручним консолідатом 8 версійних секцій; ризик — пропущена активна константа/enum при наступній зміні схеми. | При наступному schema bump (v7→v8+): перед редагуванням дайджесту звірити його enum/ACTIONS/PERMISSIONS з кодом і з ARCHITECTURE_HISTORY.md, переконатися що нічого активного не загубилось. | Цей TASK (розвантаження CLAUDE.md, 2026-05-15) |
| 4 | Косметичні текстові згадки видаленого `DocumentProcessor` у коментарях/рядках: `documentFactory.js:3,200`, `toolDefinitions.js:55`, `migrations/v4ToV5.js:43`, `caseSchema.js:78` (опис `lastProcessingContext`). | Старий компонент видалено (TASK 1), але посилання в коментарях/описах лишились. Масово правити зараз — поза scope (не функціональні, не ламають, ризик зачепити зайве). | Коли кожен із цих файлів наступного разу редагується по суті — прибрати/уточнити згадку в тому ж коміті. | TASK 1 salvage-and-decommission (2026-05-15) |
| 5 | Розбіжність clamp-логіки нарізки: legacy мав два майже-дублікати — `splitPDFByDocuments` (`startIdx = doc.startPage - 1`) і inline у `handleSplit` (`Math.max(0, doc.startPage - 1)`). Трансплантовано перший дослівно (`documentBoundary/splitPdf.js`); другий зник разом зі старим DP. | Salvage-TASK переносить дослівно, не зливає дві сутності тихо (правило #11). Захист від негативного startIdx — рішення архітектури DP v2, не salvage. | Коли DP v2 підключатиме `documentBoundary` — свідомо обрати канонічний clamp (ймовірно `Math.max(0, …)`) і покрити тестом. | TASK 1; `docs/reports/report_task_1_salvage_and_decommission.md`; pre-deletion код — git tag `pre-dp-v2-old-dp-removal` (на remote — SHA `b3b847f`) |
| 6 | Doc-drift: задокументований у CLAUDE.md/спеках перелік значень `time_entry` capture-method (`timer\|manual\|agent\|import\|legacy`) ≠ фактичні значення в коді (`instrumentation`, `manual_assign`, `manual`, `legacy_import`, + те що передає caller через `context.captureMethod`). | TASK 2 — це rename ПОЛЯ, не нормалізація значень; правити документований enum = окремий doc-TASK (CLAUDE.md заборонено чіпати в TASK 2). Перелік значень не валідується міграцією свідомо (валідація проти хибного enum зіпсувала б легітимні значення). | Окремий doc-sync TASK для CLAUDE.md (синхронізація задокументованих enum зі станом коду) — там виправити перелік. | TASK 2; `docs/reports/report_task_2_time_entry_capture_method.md` |
| 7 | `ACTIONS.add_time_entry` має мертвий рядок `const tenant = getCurrentTenant ? null : null;` — `getCurrentTenant` ніде не імпортовано (ні в колишньому App.jsx, ні в `actionsRegistry.js`). При реальному виклику `add_time_entry` це кидає ReferenceError, перехоплюється `executeAction` try/catch → `{success:false}`. `tenant` ніде не використовується (`tenantId` рахується з `getCurrentUser()`). | Латентний баг ще з App.jsx; TASK 5 — behavior-preserving винос, рядок збережено дослівно (виправлення = зміна поведінки, поза scope; інструкція TASK 5 прямо вимагає фіксувати, не виправляти). Жоден тест не виконує `add_time_entry` (тому baseline зелений). | Коли `add_time_entry` буде підключено до реального caller'а АБО прийде перший фейл-репорт по `add_time_entry` → видалити мертвий рядок (значення `tenant` не потрібне) окремим bug-fix TASK. | TASK 5 mini-аудит; `docs/reports/report_task_5_actionsregistry_refactor.md`; `actionsRegistry.js` (колишній `App.jsx:5108`) |
| 8 | Неконсистентність форми результату ACTIONS: `update_case_field` на забороненому полі повертає `{ error }` БЕЗ `success:false`, тоді як сусідні ACTIONS (`add_document`, `add_hearing`, …) повертають `{ success:false, error }`. Колишній `_actionsHarness` маскував це (додавав `success:false` від себе). | TASK 5 — behavior-preserving; нормалізація форми = зміна поведінки App, поза scope (інструкція TASK 5: дрібну неконсистентність фіксувати в tracking_debt, не виправляти). Асерт у `drag-n-drop.test.js` вирівняно на фактичну форму (`toBeFalsy`), інтент незмінний. | Окремий ACTIONS-return-shape normalization TASK (або наступного разу коли `update_case_field` редагується по суті) → стандартизувати на `{ success:false, error }` + покрити тестом. | TASK 5; `docs/reports/report_task_5_actionsregistry_refactor.md` |
| 9 | `prepareFile` (`CaseDossier/index.jsx` ~1525) після видалення drop-queue (DP-1) референсований лише мертвим блоком `{false && (…)}` старої inline-модалки. Лексично «вживається» (нема lint-помилки), фактично dead code що підтримує dead code. | DP-1 видалив drop-queue (рішення адвоката); стара inline-модалка має **власний** запланований cleanup TASK (її коментар «буде видалена окремим cleanup TASK»). Чистити `prepareFile` зараз = торкатись того `{false}` блоку = scope creep у свідомо відкладене (DEVELOPMENT_PHILOSOPHY: чистимо лише те що активує цей TASK). | Коли спрацює cleanup-TASK старої inline-модалки (видалення `{false && (…)}` блоку) — прибрати `prepareFile` тим самим комітом (HEIC-pre-step там більше нікому не потрібен; конвертацію HEIC покриває `converterService`). | TASK DP-1; `docs/reports/report_task_dp1_pipeline.md` |
| 10 | `tests/integration/drag-n-drop.test.js` — назва історична після видалення drop-queue UI (DP-1). Тест НЕ імпортує компонент: перевіряє `add_document` ACTION-контракт (null category/author/procId → ⚠-маркер `needsReview`) через `_actionsTestSetup` — валідний і зелений (AddDocumentModal/DP v2 теж покладаються на цей контракт). | Перейменування зараз = ризик без потреби (green; тестує живий ACTION-контракт, не видалений UI). Видалення drop-queue його не торкається (DP-1 підтверджено прогоном). | Наступне редагування цього файлу по суті → перейменувати у `add_document_minimal_metadata.test.js` (відв'язати назву від видаленого UI) + оновити шапку-коментар (стале посилання `CaseDossier:1942-1985`). | TASK DP-1; `docs/reports/report_task_dp1_pipeline.md` |
| 11 | Латентна #11-колізія: `tenant.settings.ecitsAutoProcess` (enum `'manual'\|'auto'`, активний свіч DP-2, читається `getEcitsAutoProcess()` + `ecitsInboxWatcher`) і пре-існуючий `tenant.settings.moduleIntegration.ecits.autoProcessIncoming` (boolean, TASK 0.2 інфра-плейсхолдер, **нуль споживачів** у коді) — два імені про один намір «чи авто-обробляти ЄСІТС-надходження». | DP-2 свідомо НЕ зливає: `autoProcessIncoming` — placeholder без жодного caller'а (та сама speculative generality що `discussion_dp_v2_philosophy_response` §Q3 «нема caller → запис у tracking_debt, не структура»). Конвергенція зараз = чистити те, що цей TASK не активує (золота середина). DP-2 додав канонічний `ecitsAutoProcess` як прямо вимагає handoff §4.5. | Коли `moduleIntegration.ecits.autoProcessIncoming` отримає першого реального споживача АБО DP-4 робитиме settings-UI ЄСІТС → звести два поля в одне (лишити `ecitsAutoProcess`, прибрати boolean-placeholder) тим же TASK. | TASK DP-2; `docs/reports/report_task_dp2_archives_ecits.md`; `tenantService.js` (`DEFAULT_TENANT.settings`) |
| 12 | Debug `console.log("executeAction OK: …", params, result)` у гарячому шляху реального `executeAction` (`actionsRegistry.js:1678`) — шумить у виводі будь-якого інтеграційного тесту що ганяє справжній `createActions` (зокрема нового `dp2-stages.test.js`). | Пре-існуючий ще з App.jsx (TASK 5 виносив behavior-preserving — рядок збережено дослівно). DP-2 його НЕ вводив і НЕ чистить (поза scope; «чисти лише те що активує цей TASK»; behavior-neutral — лише console-шум, не логіка). | Окремий debug-cleanup TASK для `actionsRegistry.js` АБО наступне редагування `executeAction` по суті → прибрати/огорнути в debug-gate цей `console.log` (і парний `:1226`). | TASK DP-2; `docs/reports/report_task_dp2_archives_ecits.md`; `actionsRegistry.js:1678` |
| 13 | `DocumentPipelineProvider`: `executor` `useMemo` залежить від `executeAction` (новий референс щорендер App, бо `createActions` живе в тілі App — TASK 5) → `streamingExecutor` пересоздається щорендер App. | Behavior-neutral: in-flight `run()` тримає старе замикання; `jobProgressStore` модульний (прогрес не губиться); `cancelledRef`/`runOptionsRef` стабільні `useRef`. Виправлення (стабілізація референсу `executeAction`/мемоізація поза App) — зміна DI-каркасу, поза scope BUGFIX. | Профілювання продуктивності DP на великому пакеті виявить накладні витрати АБО окремий TASK стабілізації `executeAction`-референсу → мемоізувати executor поза залежністю від `executeAction`-ідентичності. | DIAGNOSTIC DP-4 bugfix (2026-05-18); `docs/diagnostics/diagnostic_dp4_bugfix.md` §12 |
| 14 | Дедуп документів — metadata-евристика (`findDuplicate`: назва+pageCount/розмір), не контент-хеш (свідоме рішення адвоката: BUGFIX не додає поле/міграцію). Ризик хибно-позитивного авто-пропуску для різних документів з однаковою назвою+pageCount у різних прогонах (рідко; пом'якшено decision у «Потребує уваги»). | Контент-хеш потребує нового поля канонічної схеми + bump schemaVersion + ідемпотентної міграції — feature-розмір, прямо заборонено scope BUGFIX («НЕ робити нові фічі», «НЕ додавати поля без bump»). | Наступний schemaVersion bump → додати `contentHash` у канонічну схему + міграцію; замінити `findDuplicate` на хеш (точний дублікат → справжня автозаміна; варіант → інтерактивне «замінити/новий варіант» у DP-6). | DIAGNOSTIC DP-4 bugfix; `splitDocumentsV3.js findDuplicate`; `docs/diagnostics/diagnostic_dp4_bugfix.md` §12 |
| 15 | Перемикач PDF↔Текст у в'юері документа (CaseDossier): TXT з 02_ОБРОБЛЕНІ існує і локалізується наявним `ocrService.getCachedText` (конвенція `<basename>_<driveId>.txt`, зміна схеми НЕ потрібна), але UI його не показує поряд з PDF. | Окрема UI-задача іншого модуля (CaseDossier), не дефект потоку даних pipeline; змішувати UI-фічу в correctness-BUGFIX порушує «один TASK — одна турбота». Залежить від G1 (інакше перемикач показав би змішаний текст і виглядав би зламаним). | Тригер спрацював — G1 (page-precise TXT) у гілці BUGFIX. Окремий малий CaseDossier-TASK: кнопка PDF/Текст → `getCachedText(document)` (driveId+name), без зміни схеми. | Smart Triage BUGFIX G6 (2026-05-19); `docs/reports/report_smart_triage_bugfix.md`; `ocrService.js:51` |
| 16 | Інтеграція `cleanForReading` (Haiku-чистка для читання) зі slice не визначена: для нарізаних G1 пише СИРИЙ per-page `_text` (детерміновано, точні межі); чистка дає цілісний MD без меж сторінок — посторінкова нарізка несумісна. Зараз slice ігнорує чистку. | Адвокат НЕ вмикав чистку на прогоні Брановського (дефолт). Варіант 1 (завжди сирий per-page для slice) / 2 (чистити кожен зріз — N Haiku-викликів, дорого, підсилює bug 3) / 3 (без чистки для slice) свідомо відкладено доки сирий slicing не підтверджено на реальній справі. | Після підтвердження адвокатом коректного сирого slicing на Брановському → рішення Варіант 1/2/3 + (за потреби) чистка-per-slice окремим кроком. | Smart Triage BUGFIX G1/G6 (2026-05-19); `docs/reports/report_smart_triage_bugfix.md`; `splitDocumentsV3.js sliceProcessedArtifacts` |
| 17 | HEIC→JPEG передобробка (або візуальний fallback) ПЕРЕД image_merge маршрутом. Симптом: `HTMLImage→createImageBitmap decode failed` на конкретних файлах (паспорт громадянина України Брановського, ймовірно HEIC або PDF з image-only сторінкою низької якості). B3-фікс уже робить graceful failure (документ → decision `image_merge_failed`, pipeline продовжує), але сам декодер не отримав fallback. | B3 закрив токсичність помилки (не валить інші документи). Додавання HEIC pre-step / альтернативного декодера = окрема feature-задача, не correctness-фікс. Поки симптом локалізований одним документом-паспортом — можна жити з graceful skip. | **Повторні `image_merge_failed` decisions на HEIC АБО PDF з image-only сторінкою** на різних справах (≥2 випадки) → окремий малий TASK: lazy-import `heic2any` як у конвертері, спроба декодування JPEG fallback, лише потім graceful skip. | DP layout-leak + speed fixes Phase A B3 (2026-05-20); `docs/reports/report_task_dp_layout_leak_and_speed_fixes.md`; `splitDocumentsV3.js` image_merge гілка |
| 18 | Подвійна риска прогрес-бару у `GlobalProgressScreen.jsx` під час фази PERSIST («Розкладання документів»). Візуальний side-effect, з'явився після фіксів B1-B3 (ймовірно нова гілка state-машини для decisions додала другий ProgressBar instance). Прогрес-індикація фактично працює (модалка у фоні теж показує лічильник «Документ N з M»), але візуально неохайно. | UI-cosmetic, не блокує роботу і не ламає функціональність. Виправлення зараз = scope creep у correctness-TASK. | Наступне редагування `GlobalProgressScreen.jsx` по суті АБО окремий UI-cleanup TASK (можна об'єднати з іншими дрібними UI-фіксами модалки DP в одному PR). | DP layout-leak + speed fixes Phase A (2026-05-20); `docs/reports/report_task_dp_layout_leak_and_speed_fixes.md`; `src/components/DocumentProcessorV2/GlobalProgressScreen.jsx` |
| 19 | Збагачений дайджест паспорта меж (D1 у `TASK_smart_triage_passport_scale_and_text.md` §3.2): печатка/підпис (`visualElements`), стрибок якості (`imageQualityScores` дельта vs попередня сторінка), розрідженість (мало blocks + короткий `_text`), дельта формату/орієнтації vs попередня, зміна мови. **Зафіксовано як D1, але повністю не реалізовано** — поточний `compactDigest` у `pageMarkers.js` має тільки наявні базові сигнали + кілька з нових. Якість нарізки на тестах Брановського ~85-90% (2 прогони: 24/25 і 19-21/23 правильно). | D1 зафіксовано спекою, але адвокат рекомендував спершу довести швидкість і базову коректність нарізки. Якість «достатньо добра» поточно. Збагачення = додаткова робота над `compactDigest()` + перевірка кожного сигналу на реальних даних. | **Якщо після фази B (P1-P4) і ФД-4 propose→confirm UI якість нарізки лишається <90% або адвокат регулярно править план у Зоні 3** → окремий TASK на збагачення дайджесту 5-ма дорадчими сигналами + інтеграційні тести з реальними фіксованими сценаріями. | `TASK_smart_triage_passport_scale_and_text.md` §3.2 D1; ФД-1.1 коміт `af79086` (часткова реалізація — адаптивна щільність, не повне D1) |
| 20 | **Phase B forward-fix** — P1 (паралелізація PERSIST через `runWithConcurrency`) спричинив регресію на реальному Provider'і (`vadymlevyt.github.io/registry/`, тест на планшеті 2026-05-20): branch A (slice) опинилась у passthrough → ціла справа одним документом, перемикач Скан/Текст зник. Швидкість при цьому виграла ×2 (2-3 хв замість 5-6). 5 revert-комітів повернули main на функціональний стан Phase A. **Phase B гілка `claude/optimize-dp-pipeline-phase-b-Sw2NH` (HEAD `7491b92`) збережена на origin** для пост-mortem і forward-fix. Гіпотези кореня: (а) dedup-race `registryView() ∪ pendingInBatch` хибно ловить нарізані як дублікати; (б) зміна error-contract (`throw` → `stageError` aggregation через `runWithConcurrency __error`) не виставила `stageError` на edge-case і branch A повернула `ok:true` з 0 documents. 1423 unit-тести цього не зловили бо integration тест P1 перевіряв concurrency-ліміт, а **не якісну поведінку 25-doc плану** через справжній Provider. | Phase A швидкість прийнятна (5-6 хв на 65pp, якість виросла відносно перших прогонів). Phase B forward-fix потребує: (1) діагностика з реалістичним 25-doc планом і детермінованим mock Drive — звузити (а) vs (б); (2) виправити корінь у `splitDocumentsV3.js` (P1) і/або `concurrency.js`; (3) ОБОВ'ЯЗКОВИЙ **Provider-integration тест якісної поведінки** (інституційне обмеження №1 батьківського TASK_smart_triage) — після Provider-DP run з 25-doc планом усі 25 documents мають бути створені, branch A повністю виконана, без passthrough. P2-P4 окремо коректні і можуть бути перезапушені окремо в окремих фіксах. | Коли потрібна додаткова швидкість на великих 200-250-сторінкових томах (особливо з очисткою тексту / стисненням) + є час на діагностику з реалістичним Provider-integration тестом → окрема сесія Phase B v2 forward-fix. До того моменту Phase A швидкість прийнятна. | `docs/reports/report_task_dp_speed_phase_b_reverted.md`; revert-коміти `b6b8e62` `2a70949` `495ec56` `de12853` `88ddd09`; Phase B гілка `claude/optimize-dp-pipeline-phase-b-Sw2NH` HEAD `7491b92` |

---

Поповнюється з кожним TASK (аудити, cleanup'и, виноси). Закритий запис —
видаляється з таблиці після того, як тригер спрацював і борг сплачено
(слід лишається в git-історії та у відповідному `report_*.md`).
