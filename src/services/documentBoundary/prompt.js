// ── DOCUMENT BOUNDARY · PROMPT ───────────────────────────────────────────────
// Інституційне знання: промпт пошуку меж документів на українських судових
// сканах. Трансплантовано ДОСЛІВНО з історичного DocumentProcessor
// (analyzePDFWithDocumentBlock, src/components/DocumentProcessor/index.jsx
// :189-222) у TASK 1. Текст НЕ редагувати без свідомого рішення — снапшот-тест
// (tests/unit/documentBoundary.test.js) ловить ненавмисний дрейф.
//
// Шаблон і інтерполяція userHint — точно як legacy: порожній hint дає
// "Це PDF файл судової справи. " (з кінцевим пробілом) без блоку "Контекст:".

/**
 * Зібрати промпт пошуку меж документів.
 * @param {string} [userHint] — контекст від адвоката (опційно)
 * @returns {string} промпт дослівно як legacy
 */
export function buildBoundaryPrompt(userHint) {
  return `Це PDF файл судової справи. ${userHint ? `Контекст: ${userHint}` : ''}

Прочитай весь документ і визнач де починається кожен окремий документ.
Шукай: нові заголовки, печатки, підписи, нову нумерацію сторінок, зміну типу документа.

Поверни ТІЛЬКИ JSON без жодного тексту до або після:
{
  "totalPages": 65,
  "documents": [
    {
      "name": "Титульна сторінка судової справи",
      "startPage": 1,
      "endPage": 1,
      "type": "court_cover"
    },
    {
      "name": "Позовна заява Брановської Л.Б.",
      "startPage": 2,
      "endPage": 8,
      "type": "pleading"
    }
  ]
}

Типи документів (type):
- court_cover: титульна сторінка справи
- pleading: позовна заява, відзив, заперечення
- court_act: ухвала, рішення, постанова суду
- evidence: докази, додатки, довідки
- certificate: свідоцтво, витяг з реєстру
- contract: договір, угода
- other: інше

ВАЖЛИВО: визначай межі тільки на основі реального вмісту. Не вигадуй документи яких немає.`;
}
