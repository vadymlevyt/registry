# Handoff — TASK 3 «Очищення тексту» (нова сесія)

**Дата:** 2026-05-31
**Це продовження** роботи попередньої координаційної сесії (relay-тригер, винос дублів,
консолідація UI-боргів). Ти береш **окрему функціональну вісь** — TASK 3, очищення тексту.

---

## 1. ЩО ПРОЧИТАТИ НА СТАРТІ (економно — лише це)

**Обовʼязково:**
- `CLAUDE.md` + `DEVELOPMENT_PHILOSOPHY.md` (правило старту проєкту).
- `docs/consultations/consultation_clean_text_design.md` — **SSOT дизайну TASK 3** (продуктовий
  намір адвоката, зафіксований до спеки). Головний документ.

**Як зразок патерну (спільний сервіс для DP + вкладки):**
- `docs/tasks/TASK_context_generator_unify.md` + `docs/reports/report_task_context_generator_unify.md`
  — контекст-генератор зроблено САМЕ так, як треба зробити clean-text: один спільний сервіс,
  що його кличуть і Document Processor, і вкладка. **Дзеркаль цей патерн.**

**Не читати на старті** (щоб не засмічувати контекст): решту roadmap-файлів, історію дублів/relay.
Звернутися лише за потреби.

---

## 2. ЩО МАЄ ЗРОБИТИ TASK 3 (з опису адвоката; деталі — у design-файлі)

- Функція **очищення тексту** документа: брати **layout-метадані, які повертає Document AI**
  (абзаци, форматування) → формувати чистий **Markdown** (а не сирий OCR-потік).
- Додати **кнопку у вкладці «Огляд»** (CaseDossier) — запуск очищення для документа справи.
- Реалізувати **одразу як СПІЛЬНИЙ сервіс** для Document Processor і для вкладки «Огляд» —
  **не дублювати** (правило #11 / #30, Rule of Three з самого народження, як контекст-генератор).
  НЕ робити окрему копію в DP і окрему в Огляд.

> Точні кроки, формат Markdown, звідки беруться layout-метадані (`.layout.json` поряд з OCR),
> UX кнопки — у `consultation_clean_text_design.md`. **Перед кодом — перезвір дизайн** (код міг
> змінитись з моменту написання; якщо так — онови розуміння, можливо міні-аудит).

---

## 3. МЕЖІ ФАЙЛІВ (анти-колізія — паралельно йдуть інші сесії)

Працюють ще дві сесії. Щоб не зіткнутись на файлах (план:
`docs/consultations/consultation_ui_debt_consolidation_plan.md`, смуга **C** — твоя):

- **Твоє:** новий clean-text сервіс (новий файл у `src/services/...`), кнопка у `CaseDossier`
  вкладка «Огляд», точка виклику в `DocumentProcessorV2/index.jsx`.
- **НЕ чіпати:** `DpImageMergeEditor.jsx`, `ImageMergePanel/`, `ImageEditor/` (їх веде UI-debt
  сесія, смуга B), `App.jsx` (сесія ai_usage guard, смуга A).
- `CaseDossier` — спільна зона з майбутньою «контекст-частиною прогрес-бару»; ти додаєш кнопку в
  Огляд, це ок; координатор зведе по черзі.

---

## 4. ПРОЦЕС

- Harness видасть власну робочу гілку `claude/*` — працюй на ній, коміть, пуш.
- **НЕ зводь у main сам.** Як закінчиш — повідом; координатор (ця сесія) звірить код за патерном
  «спільний сервіс, нуль дублювання» і зробить FF у main (як з relay/дублями). main свіжий
  (relay + дублі + доки).
- Тести: юніт на новий сервіс + інтеграція якщо торкається ACTIONS; `npm test` зелений; build OK.
- Звіт наприкінці: `docs/reports/report_task3_clean_text.md`.

---

## 5. КОНТЕКСТ — СТАН НА 2026-05-31

- ✅ У main: контекст-генератор (естафетний relay), спільна логіка дублів модалка↔DP, таск-доки.
- 🔄 в роботі: ai_usage write-guard (сесія AI-cost, смуга A); консолідація UI-боргів (смуга B).
- 🟡 Відкладені борги: #33 (решта спільних хелперів), #34 (прогрес-бар), #36/#28 (drag між групами)
  — усі в смузі B, не твоя зона. (#35 — глибший корінь втрати ai_usage, смуга A.)
- Твоя вісь — TASK 3, перший фронт 5-фазного плану
  (`docs/consultations/consultation_combined_roadmap_dp_and_refactoring.md` — лише за потреби).
