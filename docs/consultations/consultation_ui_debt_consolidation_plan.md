# План консолідації UI-боргів — image editor / Document Processor

**Дата:** 2026-05-31
**Тип:** план / orientation (НЕ TASK-спека)
**Контекст:** після виносу контекст-генератора (relay) і спільної логіки дублів накопичились
суміжні UI-борги. Рішення адвоката: розгрібати їх організовано, а не плодити далі.

---

## Борги, що зводимо

| Борг | Суть | Файли |
|------|------|-------|
| #33 | Решта дубльованої логіки модалка↔DP (previewUrls, cropState, лічильники банера, flatPositions) | `ImageEditor/`, `ImageMergePanel/`, `DpImageMergeEditor.jsx` |
| #36 (+#28) | Вільне перетягування фото між групами вкл. порожні + допилити add-group; механізм = drop-on-container | `DpImageMergeEditor.jsx` |
| #34 | Спільний індикатор прогресу довгих операцій (фото-обробка + контекст-генератор) | `DpImageMergeEditor.jsx` (фото) + `CaseDossier/...contextGenerator` (контекст) |

---

## ПРИНЦИП: смуги володіння файлами (анти-колізія)

Зараз потенційно ТРИ сесії працюють паралельно. Щоб НЕ повторити плутанину з гілками —
кожен файл має **рівно одного власника** в конкретний момент:

| Смуга | Файли (володіння) | Хто | Зміст |
|-------|-------------------|-----|-------|
| **A** | `src/App.jsx` | сесія AI-cost | ai_usage write-guard (окремо) |
| **B** | `DpImageMergeEditor.jsx`, `ImageMergePanel/`, `ImageEditor/` | UI-debt сесія (ця гілка) | #33 + #35 + фото-частина #34 |
| **C** | новий clean-text сервіс, кнопка в `CaseDossier` Огляд, `DocumentProcessorV2/index.jsx` | нова сесія TASK 3 | очищення тексту |

**Жорсткі межі:**
- Смуга B **НЕ** чіпає `App.jsx` і `DocumentProcessorV2/index.jsx` (тільки `DpImageMergeEditor.jsx`).
- Смуга C **НЕ** чіпає `DpImageMergeEditor.jsx` / `ImageMergePanel/` / `ImageEditor/`.
- **Перетин по `CaseDossier`** (C додає кнопку в Огляд; контекст-частина #34 теж у CaseDossier) —
  тому контекст-частину #34 робимо **після** того, як C відпустить CaseDossier (або окремим
  кроком, коли CaseDossier вільний). Фото-частина #34 — у смузі B, перетину немає.
- Усе через FF у main по черзі (як relay/дублі); координатор зводить, перевіривши код.

---

## Розподіл по тасках (смуга B — ця сесія)

Усе орбітує `DpImageMergeEditor.jsx`, тож **серіально в одній сесії** (не паралельні гілки на
одному файлі), окремими коммітами:

- **B1 — Винос решти спільних хелперів (#33).** Фаза 1 (чисті: cropState/лічильники/flatPositions/
  uncertainSet) + Фаза 2 (`usePreviewUrls`). **Першим** — дає чисту дедупльовану базу під фічу.
  Спека: `docs/tasks/TASK_imageeditor_share_remaining_helpers.md` (перед стартом — перезаудит §0).
- **B2 — Вільне перетягування між групами + add-group (#36/#28).** Фіча на очищеній базі:
  `useDroppable` per group (вкл. порожні), `DragOverlay`, add-group необмежено й придатно як ціль.
- **B3 — Прогрес фото-обробки (фото-частина #34).** Спільний індикатор-компонент, підключений до
  фаз `DpImageMergeEditor` startup. Будувати ОДРАЗУ спільним (правило #30).

**Контекст-частина #34** — окремий короткий крок поза смугою B (CaseDossier), після TASK 3.

### Послідовність
B1 → B2 → B3, один відкритий DP. Перед кожним кроком — звірка зі свіжим main; FF у main з
підтвердженням (code change). Тести зелені на кожному кроці.

---

## Звʼязок борг → таск
- #33 → B1
- #36 + #28 → B2
- #34 → B3 (фото) + окремий крок (контекст, поза B)

## Що НЕ входить
- TASK 3 (очищення тексту) — окрема функціональна вісь, смуга C, нова сесія (handoff
  `docs/consultations/handoff_2026-05-31_clean_text_task3.md`).
- ai_usage guard — смуга A, сесія AI-cost.
