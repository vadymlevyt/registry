# Об'єднаний процес — DP продуктова візія + хаос cleanup + структурний рефакторинг

Дата: 2026-05-28 (v2 — додано третю вісь)
Призначення: показати як **три осі робіт** йдуть **одним процесом** паралельно
Зв'язані документи:
- `docs/consultations/consultation_dp_product_vision.md` (DP продуктова вісь)
- `docs/diagnostics/diagnostic_dpv2_call_trace_and_chaos.md` (хаос-вісь)
- `docs/consultations/consultation_large_files_refactoring_roadmap.md` (рефакторингова вісь)
- `docs/consultations/dp_reuse_and_canonical_patterns_discussion.md` (Rule of Three, code reuse)

---

## 1. Три осі — навіщо ставити їх в один процес

```
─────────────────────────────────────────────────────────────────────────
  ВІСЬ A: ПРОДУКТОВА — DP від поточних 40% точності до повної візії
─────────────────────────────────────────────────────────────────────────
  Що рухає: візія `consultation_dp_product_vision.md`
            Адвокат закидає → AI пропонує → ★ адвокат править план ★ → виконує
  5 фаз: інфраструктура → тумблери → UI плану → якість → розширення
  Без неї:  DP лишається box з лотереєю, користі мінімум

─────────────────────────────────────────────────────────────────────────
  ВІСЬ B: ХАОС — 10 точок неузгодженості з diagnostic
─────────────────────────────────────────────────────────────────────────
  Що рухає: 10 точок з `diagnostic_dpv2_call_trace_and_chaos.md`
            (C1: tool use; C2: AI helpers у Provider; C3: strip RAM;
             C4: AddModal уніфікація; C5: мертвий persistStage;
             C6: _temp плоска; C7: AI usage діра; C8: lazy import)
  Без неї:  кожен новий TASK дубль хаос, технічний борг росте

─────────────────────────────────────────────────────────────────────────
  ВІСЬ C: РЕФАКТОРИНГ — великі файли → split
─────────────────────────────────────────────────────────────────────────
  Що рухає: `consultation_large_files_refactoring_roadmap.md`
            ImageMergePanel (✓ виконано), AddDocumentModal, CaseDossier,
            Dashboard, App.jsx
  Без неї:  читання великих файлів стає дорогим, нові розробники тонуть
```

**Чому ці три осі НЕ можна вести як три окремі плани:**

- **DP без хаос-cleanup** = накопичуємо борг швидше ніж розгрібаємо. Нові тумблери лежать поруч з мертвим persistStage і дубльованими AI helpers.
- **Хаос-cleanup без DP-функцій** = марна робота. Адвокат не бачить нічого, лише «всередині стало чистіше».
- **Refactor без DP/хаосу** = переїзд коду без розв'язання структурних проблем (DrivePicker дублюється, AddModal паралельний з DP).
- **DP без refactor** = додаємо UI плану у вже-перевантажений CaseDossier (3260 рядків).

Тобто три осі — це **один процес**, де кожен TASK може зачіпати одну, дві або всі три осі **одночасно**.

---

## 2. Об'єднаний таймлайн (три осі паралельно)

```
═══════════════════════════════════════════════════════════════════════
  Фаза 0 — ЗАКРІПЛЕННЯ (зараз)
═══════════════════════════════════════════════════════════════════════
  A) Зафіксувати DP візію у репо ✓ (комітнуто 2026-05-28)
  B) Зафіксувати хаос-діагностику ✓ (комітнуто 2026-05-28)
  C) Зафіксувати об'єднаний roadmap ✓ (цей файл, v2)

═══════════════════════════════════════════════════════════════════════
  Фаза 1 — ФУНДАМЕНТ (тиждень 1-2)
═══════════════════════════════════════════════════════════════════════
  ┌──────────────────────────────────────────────────────────────────┐
  │ TASK_dp_tool_use_migration                                       │
  │ ──────────────────────────                                       │
  │  A) DP tool use migration (analyzeViaToolUse →                   │
  │       runMultiTurnConversation + DOC_PROCESSOR_TOOLS)             │
  │  B) C3 «strip image/tokens у ocrChunkBytes» — по дорозі           │
  │  B) C7 «логування AI usage в aiCleanText/aiReconstructFile»       │
  │      — по дорозі                                                  │
  │  B) C8 «статичний import analyzeTriageViaToolUse» — побіжно       │
  │  C) нічого (їхня Фаза 1 ImageMergePanel уже виконана)             │
  │                                                                   │
  │ Очікувані файли: analyzeTriageViaToolUse.js, analyzeViaToolUse.js, │
  │                  DocumentPipelineContext.jsx (Provider helpers),  │
  │                  toolDefinitions.js (новий DOC_PROCESSOR_TOOLS).   │
  │ 2-3 дні.                                                          │
  └──────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════
  Фаза 2 — ТУМБЛЕРИ DP (тиждень 3-7, по черзі один TASK на тумблер)
═══════════════════════════════════════════════════════════════════════
  ┌──────────────────────────────────────────────────────────────────┐
  │ TASK_dp_F7_organize_by_proceedings   ← 1-2 дні                   │
  │  A) AI пропонує розкладку по провадженнях за датою/категорією    │
  │  B/C) не зачіпає                                                  │
  └──────────────────────────────────────────────────────────────────┘
  ┌──────────────────────────────────────────────────────────────────┐
  │ TASK_dp_F8_integrity_check           ← 2-3 дні                   │
  │  A) перевірка пропусків сторінок, дублікатів                     │
  │  B/C) не зачіпає                                                  │
  └──────────────────────────────────────────────────────────────────┘
  ┌──────────────────────────────────────────────────────────────────┐
  │ TASK_dp_F10_generate_summary         ← 2-3 дні                   │
  │  A) AI пише короткий висновок про документ (Haiku)               │
  │  B) AI helpers — координація з C2 (вже у Фазі 4 нижче)            │
  │  C) не зачіпає                                                    │
  └──────────────────────────────────────────────────────────────────┘
  ┌──────────────────────────────────────────────────────────────────┐
  │ TASK_dp_F12_suggest_deadlines        ← 3-5 днів                  │
  │  A) витягує дедлайни з тексту + пропонує у Календар              │
  │  B/C) не зачіпає                                                  │
  └──────────────────────────────────────────────────────────────────┘
  ┌──────────────────────────────────────────────────────────────────┐
  │ TASK_dp_F13_update_case_context      ← 5-7 днів                  │
  │  A) DP викликає contextGenerator після обробки                   │
  │  B) AI helper для генерації — або у dp-ai/, або у CaseDossier/    │
  │      services/. ★ КООРДИНАЦІЯ З C: за refactor roadmap (§6.6)     │
  │      contextGenerator.js живе у CaseDossier/services/, бо це      │
  │      сервіс досьє. DP лише викликає через імпорт або eventBus.    │
  │  C) створюється папка CaseDossier/services/ (частина їхньої       │
  │      майбутньої Фази 3 CaseDossier split — авансом)               │
  └──────────────────────────────────────────────────────────────────┘
  ┌──────────────────────────────────────────────────────────────────┐
  │ TASK_dp_F14_fill_case_card           ← 5-7 днів                  │
  │  A) AI заповнює сторони / суд / статтю КК                        │
  │  B/C) не зачіпає                                                  │
  └──────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════
  Фаза 3 — ★ ТРОЇСТИЙ TASK: AddModal split + DrivePicker + DP-C4 ★
            (тиждень 8) — головний приклад «одна робота — три осі»
═══════════════════════════════════════════════════════════════════════
  ┌──────────────────────────────────────────────────────────────────┐
  │ TASK_addmodal_split_unify_with_dp                                │
  │ ──────────────────────────────────                               │
  │  A) DP-C4: уніфікація AddDocumentModal з DP pipeline              │
  │      Single-file шлях через streamingExecutor (1 файл = 1-pass)  │
  │      Видалити паралельний OCR код у AddModal                      │
  │  B) C6 «DrivePicker дублікат» — винос у                          │
  │      components/UI/DrivePicker/ (рівноправний споживач для       │
  │      DP v2 і AddModal)                                            │
  │  C) Refactor Фаза 2: AddDocumentModal split 1039 → 330 + 5       │
  │      файлів (за їхнім роадмепом §8)                              │
  │                                                                   │
  │ 3-5 днів. Один TASK, три осі.                                    │
  └──────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════
  Фаза 4 — CLEANUP перед UI плану (тиждень 9)
═══════════════════════════════════════════════════════════════════════
  ┌──────────────────────────────────────────────────────────────────┐
  │ TASK_dp_internal_cleanup                                         │
  │ ────────────────────────                                         │
  │  A) не зачіпає                                                    │
  │  B) C2: винести AI helpers з DocumentPipelineContext у           │
  │      src/services/dp-ai/ (triageClient, cleanupClient,            │
  │      reconstructClient). Узгоджено з §3 reuse-discussion.        │
  │  B) C5: видалити мертвий DP-1 persistStage з диригента,          │
  │      документувати INTAKE/CONVERT як passthrough                  │
  │  B) C6 (можливо): структуровані теки _temp/<job>/chunks/,        │
  │      originals/, state/                                           │
  │  C) не зачіпає                                                    │
  │                                                                   │
  │ 1-2 дні.                                                          │
  └──────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════
  Фаза 5 — ★ ТРОЇСТИЙ TASK: UI плану + image-merge + ImageEditor ★
            (тиждень 10-13) — найбільша робота
═══════════════════════════════════════════════════════════════════════
  ┌──────────────────────────────────────────────────────────────────┐
  │ TASK_dp_zone3_plan_editing                                       │
  │ ──────────────────────────                                       │
  │  A) DP-Фаза 3: розділити propose і execute                       │
  │      CONFIRM stage стає реальним гейтом (не passthrough)         │
  │      План зберігається в _temp/plan.json (resume support)        │
  │      Zone 3 propose-mode UI:                                      │
  │        - дерево пропонованих документів                          │
  │        - drag-and-drop редагування (об'єднати/розділити)         │
  │        - rename, type-picker                                      │
  │        - image-merge UI: drag фото між групами в одному документі│
  │        - попап crop/rotate для фото                              │
  │      Кнопка «Виконати» → продовжити pipeline з PERSIST            │
  │  B) C2 (продовження): нові AI клієнти кладуться у dp-ai/         │
  │  C) ★ Rule of Three триггер ★ — створюємо:                       │
  │      components/ImageEditor/      ← винос з ImageMergePanel/      │
  │        ├── PreviewPopup.jsx                                       │
  │        ├── CropperHost.jsx                                        │
  │        ├── Thumbnail.jsx                                          │
  │        └── grid/                                                  │
  │      services/imageDocument/      ← винос                         │
  │        ├── pdfRebuild.js                                          │
  │        └── geometry.js                                            │
  │      ImageMergePanel оновлює свої імпорти. DP-UI імпортує звідти. │
  │      (Це їхня «Фаза 1.5» — активується саме тут)                 │
  │                                                                   │
  │ 5-7 днів. Найбільший TASK у roadmap. Може ділитись на 2-3 під-   │
  │ TASK'и (наприклад: propose/execute split → propose UI → image-   │
  │ merge UI).                                                        │
  └──────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════
  Фаза 6 — ЯКІСТЬ + РОЗШИРЕННЯ (тиждень 14+)
═══════════════════════════════════════════════════════════════════════
  ┌──────────────────────────────────────────────────────────────────┐
  │ TASK_dp_quality_large_volumes                                    │
  │  A) DP-Фаза 4: sliding window АБО Sonnet/Opus для >70 стор.      │
  │     (рішення приймається з огляду на серверний перехід)          │
  │  B) C8: можливо вже зроблено, інакше зараз                       │
  │  C) не зачіпає                                                    │
  │ 1-2 тижні                                                         │
  └──────────────────────────────────────────────────────────────────┘
  ┌──────────────────────────────────────────────────────────────────┐
  │ TASK_dp_extensions                                                │
  │  A) DP-Фаза 5: ZIP unpack (createIntakeWithUnpack активувати),   │
  │     RTF/ODT в converter, Resume UI                                │
  │  B/C) інкрементально                                              │
  │ 1-2 тижні, по черзі                                               │
  └──────────────────────────────────────────────────────────────────┘

═══════════════════════════════════════════════════════════════════════
  ПАРАЛЕЛЬНО (можна виконувати незалежно від DP, інша сесія)
═══════════════════════════════════════════════════════════════════════
  ┌──────────────────────────────────────────────────────────────────┐
  │ Refactor Фаза 3: CaseDossier/index.jsx split (3260 → 900)        │
  │   - tabs/OverviewTab, MaterialsTab, DocworkTab (wrapper для DP),  │
  │     PositionTab, TemplatesTab                                     │
  │   - panels/AgentPanel (агент досьє з DOSSIER_AGENT_TOOLS),       │
  │     StorageSection                                                │
  │   - services/ — contextGenerator якщо ще не створено              │
  │   ★ КООРДИНАЦІЯ: якщо DP-Ф13 (Фаза 2 цього плану) вже зробив     │
  │     contextGenerator у CaseDossier/services/ — переїзд            │
  │     відбувається там же                                           │
  │   5-7 днів                                                        │
  └──────────────────────────────────────────────────────────────────┘
  ┌──────────────────────────────────────────────────────────────────┐
  │ Refactor Фаза 4: Dashboard/index.jsx split (2710 → 1100)         │
  │   - SlotsColumn, CalendarPanel, TimePicker (дублікат)            │
  │   - не зачіпає DP                                                 │
  │   3-5 днів                                                        │
  └──────────────────────────────────────────────────────────────────┘
  ┌──────────────────────────────────────────────────────────────────┐
  │ Refactor Фаза 5: App.jsx split (5357 → 1200)                     │
  │   - винос ACTIONS, eventBus init, providers, тощо                │
  │   - не зачіпає DP логіку (лише імпорти, обгортки)                │
  │   1-2 тижні                                                       │
  └──────────────────────────────────────────────────────────────────┘
```

---

## 3. Координаційні точки (де три осі пересікаються)

| # | Місце | Осі | Як координуємо |
|---|-------|-----|----------------|
| 1 | `contextGenerator.js` для Ф13 | A+C | Кладемо у `CaseDossier/services/contextGenerator.js` (за refactor §6.6). DP викликає через імпорт або через eventBus подію (`DOCUMENT_BATCH_PROCESSED`). НЕ кладемо у `services/dp-ai/`. |
| 2 | AddDocumentModal паралельний з DP | A+B+C | **Об'єднано в один TASK** (Фаза 3 цього плану). DP-C4 + DrivePicker винос + AddModal split — три осі в одному TASK. |
| 3 | DrivePicker дублікат | B+C | Винос у `components/UI/DrivePicker/` при AddModal split. Обидва модулі імпортують звідти. |
| 4 | Image-merge UI для DP-Фази 3 | A+B+C | **Об'єднано в один TASK** (Фаза 5 цього плану). DP-UI плану + ImageEditor винос + image-merge функціонал — три осі. Rule of Three триггер. |
| 5 | DocworkTab у CaseDossier split | A+C | DocworkTab — тонкий wrapper навколо `<DocumentProcessorV2/>` ~80 рядків. Не торкає DP. |
| 6 | Агент досьє → DP як tool | A+C | Коли CaseDossier split → AgentPanel виноситься у `panels/`. Тоді легше додати DOC_PROCESSOR_TOOLS у DOSSIER_AGENT_TOOLS — DP стає інструментом для агента (друга точка входу з DP-візії). |

---

## 4. Заборонені перетинання (не робити в окремих TASK'ах)

Наступні **не повинні** виконуватись як окремі TASK'и — їх **об'єднати** в одному:

- ❌ AddModal split окремо від DP-C4: переробка AddModal без виправлення дублювання з DP — марна.
- ❌ DrivePicker винос окремо від AddModal split: видно тільки при split.
- ❌ ImageEditor винос окремо від DP-UI плану: Rule of Three без реального споживача = overengineering.
- ❌ DP-Фаза 3 (UI плану) без винесення ImageEditor: дублювання `pdfRebuild` коду.

---

## 5. Конкретний наступний крок

Якщо погодите з цією v2 версією roadmap:

1. ✓ **DP візія в репо** — вже комітнуто (`consultation_dp_product_vision.md`).
2. ✓ **Хаос-діагностика в репо** — вже комітнуто (`diagnostic_dpv2_call_trace_and_chaos.md`).
3. ✓ **Цей об'єднаний roadmap v2 в репо** — після коміту цієї версії.
4. 📋 **Готую TASK_dp_tool_use_migration** з C3+C7+C8 «по дорозі» — перший практичний TASK Фази 1. Передається новій сесії-виконавцю як завжди (через /tmp).
5. 📋 **Скоординовану нотатку у `consultation_large_files_refactoring_roadmap.md`** — посилання на цей файл з вказанням точок координації (Фаза 2 AddModal split = троїстий TASK, Фаза 1.5 ImageEditor = троїстий TASK з DP-UI).

---

## 6. Що це міняє в роботі двох сесій (моя і адмін-чат)

**Раніше:**
- Моя сесія писала про DP (продуктова вісь + хаос cleanup)
- Адмін-чат сесія писала про refactor великих файлів
- Сесії не знали одна про одну, документи окремі

**Тепер (з v2):**
- Один **процес** з трьома осями
- Кожна сесія, що пише TASK-специфікацію, **посилається на цей файл** для координації
- Великі координаційні точки (Фаза 3, Фаза 5) — **один TASK на три осі**, не три окремі
- Малі точки (Ф13 contextGenerator) — координація через посилання у TASK-специфікації

Цей файл стає **головним координаційним документом** між обома сесіями і будь-якими майбутніми. Він не вимагає, лише **орієнтує**.

---

## 7. Що НЕ є завданням цього roadmap

- **Не вимога** — це орієнтир. Адвокат може переставляти пріоритети.
- **Не зобов'язання строків** — тижні приблизні, кожен TASK дає свою оцінку у власній специфікації.
- **Не заміна TASK-специфікацій** — кожен TASK має власний документ з SAAS/BILLING/AI USAGE розділами (правило #10).
- **Не заборона паралельності** — refactor Фази 3/4/5 (CaseDossier/Dashboard/App.jsx) можна вести **іншою сесією** паралельно з DP-roботою.

---

**Кінець v2.**

Документ-зведення трьох осей. Оновлюється коли проект просувається — кожен виконаний TASK позначається ✓ у відповідній фазі.
