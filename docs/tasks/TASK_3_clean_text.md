# TASK 3 — clean_text (Очищення тексту → Markdown), смуга C — PARENT ОГЛЯД

**Дата:** 2026-05-31
**Статус:** розбито на 3 фази (узгоджено з адвокатом 2026-05-31). Цей файл — **огляд+мапа**,
спільні рішення для всіх фаз. Деталі — у фазових файлах.
**Базові документи:**
- `docs/consultations/consultation_clean_text_design.md` — SSOT продуктового наміру
- `docs/mermaid/flow_clean_text.md` — **узгоджений алгоритм** (5 діаграм, адвокат 2026-05-31)
- `docs/tasks/TASK_context_generator_unify.md` + звіт — патерн-зразок «спільний сервіс, DI, C7»

---

## МЕТА (загальна)

Перетворити сирий OCR-текст сканованого документа (`.txt`) на **гарний читабельний
Markdown** (`.md`) — абзаци, заголовки, жирний шрифт, таблиці, близько до оригіналу —
**не змінюючи юридичний зміст**. Зробити це **спільним сервісом з народження**
(Rule of Three / #11), що його кличуть кілька точок. Паралельно винести механіку
вибору файлів з Архіву у спільний компонент для пакетних операцій у реєстрі.

---

## МАПА ФАЗ

| Фаза | Файл | Суть | Видимий результат | Деплой |
|------|------|------|-------------------|--------|
| **3.1** | `TASK_3.1_clean_text_core.md` | Ядро `cleanTextService` + DP-консолідація + viewer `.md` + schemaVersion 10 | DP-тумблер дає гарний `.md` через спільне ядро; viewer показує | окремий |
| **3.2** | `TASK_3.2_clean_text_buttons.md` | Кнопки ретроактивної очистки (Огляд N-док., Viewer один) + ACTION агента | очистка наявних справ заднім числом + голосом/агентом | окремий |
| **3.3** | `TASK_3.3_file_selection_delete.md` | Винос UI-вибору з `ArchiveView` + реєстр-мультивибір + пакетне видалення | пакетні операції в реєстрі; Архів рефакторено непомітно | окремий |

**Залежності (однонапрямлені):**
```
3.1 (ядро) ──> 3.2 (кнопки тягнуть ядро)
   │
   └─────────> 3.3 «Очистити вибрані» тягне ядро; решта 3.3 (вибір+видалення) незалежна
```
3.3 майже автономна (вибір+видалення — UI-вісь), лише дія «Очистити вибрані» спирається
на ядро 3.1. Можна робити 3.3 паралельно/після 3.1, не обов'язково після 3.2.

---

## ДВІ ОСІ (наскрізні)

**Вісь A — логіка очистки** (ядро `cleanTextService.js`): фази 3.1 (ядро+DP), 3.2 (кнопки).
**Вісь B — інтерфейс вибору файлів** (спільний компонент з `ArchiveView`): фаза 3.3.

Осі незалежні: вибір/видалення файлів НЕ стосується алгоритму очистки — інтерфейсна
нагрузка, об'єднана в TASK бо зручно (раз робимо мультивибір для очистки — той самий
вибір дає пакетне видалення).

---

## СКОУП (наскрізний, перевірено по коду — критично для ВСІХ фаз)

Очистка працює **ВИКЛЮЧНО** для `documentNature==='scanned'` (скани/фото через Document AI —
мають `.layout.json`/`mergeLayout`). **`searchable` (DOCX/HTML/текстовий PDF) — ПОВНІСТЮ
ПОЗА ФУНКЦІЄЮ** (кнопка неактивна / цикл пропускає).

Причина (по коду): `.layout.json` створює ТІЛЬКИ Document AI; `pdfjsLocal` за дизайном
не дає `pageStructure` (`ocr/pdfjsLocal.js:8`). У searchable вже чистий цифровий текст —
чистити нема чого.

| Формат | documentNature | `.layout.json` | Очистка |
|--------|---------------|----------------|---------|
| Скан PDF/фото (Document AI) | `scanned` | ✅ | ✅ |
| Фото склеєні→PDF (DP) | `scanned` | ✅ | ✅ |
| DOCX / HTML / текстовий PDF | `searchable` | ❌ | ❌ ігнор |

---

## СПІЛЬНІ АРХІТЕКТУРНІ РІШЕННЯ (наскрізні)

### Гібрид 3 кроки (Вісь A, реалізується у 3.1, переюз у 3.2)
1. **Конденсатор** (детермінований, 0 токенів AI): layout → компактна Markdown-чернетка.
   Читає **ПОСТОРІНКОВО через `page._text`** (offset'и в глобальний `.txt` ненадійні на
   сканах >25 стор. — `documentAi.js:428-435` перебазовує лише `pageNumber`). Структуру
   виводить з геометрії `boundingPoly`+розмір. Дзеркало `pageMarkers.js:80-128`.
2. **AI-поліш** (Haiku, консервативний): чернетка → фінальний `.md` + `attentionNotes[]`.
   НЕ міняє зміст (цитати/суми/дати/імена) — §6 design. JSON depth-counter парсинг.
3. **Fallback**: немає layout (старий скан) → плоский `.txt` → AI. Немає API-ключа/AI кинув
   → зберегти чернетку (deterministic-only) + warning.

### Долі артефактів (Вісь A)
- `<basename>_<id>.md` — **створюється** у 02_ОБРОБЛЕНІ;
- `<basename>_<id>.txt` — **переміщається** у `02_ОБРОБЛЕНІ/_raw_txt/` (latin-safe ім'я, #8);
- `<basename>_<id>.layout.json` — **видаляється назавжди** (паливо відпрацювало);
- метадані: `textFormat:'md'`, `cleanedAt`, `attentionNotes`.

### schemaVersion 10 (рішення для 3.1 — structural, #6)
Поля `textFormat`(`'txt'|'md'`, default `'txt'`) і `cleanedAt` (nullable) у канонічну
`cases[].documents[]` = **bump schemaVersion 10** + `migrateToVersion10` (ідемпотентна,
default `textFormat='txt'` усім) + бекап `_backups/`. `attentionNotes` — **важке поле**,
у `documents_extended.json` (bump НЕ потребує).
> Один сенс (#11): `textFormat` — «у якому форматі лежить витягнутий текст». НЕ плутати
> з `documentNature` (природа файла) і `status` (lifecycle).
**Узгоджено з адвокатом ПЕРЕД 3.1** (structural — правило #6).

### C7-логування (Вісь A, дзеркало contextGenerator)
- agentType `textCleaner` у `SYSTEM_DEFAULTS` (`modelResolver.js`) → `'claude-haiku-4-5-20251001'`.
- `logAiUsage({agentType:'text_cleaner', context:{caseId, module, operation:'clean_text', documentId}})`
  + `activityTracker.report('agent_call', ...)` — один шлях на всіх споживачів.
- **Прапор `billAsUserAction`** (default true для кнопок/ACTION; DP передає false —
  автоматичне продовження обробки, як рішення contextGenerator). Один сенс (#11):
  «чи нараховувати виклик як окрему оплачувану дію адвоката». Токени в `ai_usage[]`
  пишуться завжди; `activityTracker` як дія — лише при true.

---

## ВІДОМА WIP-ЧЕРНЕТКА (для 3.1)

На робочій гілці є **незакомічена в main** `src/services/cleanTextService.js` + тест від
попередньої сесії — **ЗЛАМАНІ**: імпортують `LAYOUT_BLOCK_TYPES`/`isNoiseBlock` з
`ocrService.js` (таких експортів немає), очікують вигаданий формат `{type,text}` замість
реального Google-shape. Логіка конденсації (normalizeInlineText, tableToGfm) — цінна,
**переписати reader під реальний `_text`+геометрія**, тест адаптувати. Деталі — у 3.1.

---

## ЗАБОРОНЕНО (наскрізне)

- ❌ Очищати `searchable`; тягнути `.layout.json` для нього (його немає).
- ❌ Offset'и абзаців у глобальний `.txt` (ламається на >25 стор.) — тільки `_text` посторінково.
- ❌ AI міняти зміст документа (консервативність — головне).
- ❌ Кирилиця в `q=` Drive API (#8) — `_raw_txt/` latin-safe.
- ❌ Чіпати `DpImageMergeEditor`, `ImageMergePanel/`, `App.jsx` (смуги A/B).
- ❌ re-OCR одного документа за сумнівом (§5 design) — наступний крок, борг.
- ❌ Дублювати логіку очистки між споживачами / CSS-механіку вибору між точками.

---

## ПОРЯДОК ВИКОНАННЯ

1. **3.1** першою (фундамент; ядро без споживача — мертвий код, тому одразу + DP + viewer).
   schemaVersion 10 узгодити на старті.
2. **3.2** після 3.1 (кнопки тягнуть готове ядро).
3. **3.3** після 3.1 (можна паралельно з 3.2; «Очистити вибрані» тягне ядро, решта автономна).

Кожна фаза: окрема сесія-виконавець, тести зелені, окремий деплой, окрема перевірка адвокатом.

**Кінець PARENT-огляду TASK 3.**
