# Flow: посторінкове (пачкове) очищення тексту — TASK 3.1 (доповнення)

**Дата:** 2026-06-01
**Контекст:** уточнення до `flow_clean_text.md` після обговорення з адвокатом.
Очищення працює **на одному готовому документі**, а всередині документа —
**пачками по сторінках** (бо весь документ за одну відповідь AI не виходить).
Споріднено з дослідженням щільності `TASK_smart_triage_passport_scale_and_text.md`
(кирилиця ~2 символи/токен; ~1000-2000 токенів/стор.; вікно Haiku 200K; стеля
виходу Haiku 4.5 = 64K).

---

## Два РІЗНІ бюджети (чому потрібні пачки)

```mermaid
flowchart LR
    IN["ВХІД — читання<br/>контекстне вікно Haiku 200K<br/>зона якості ~120K<br/>~60-80 стор. повного тексту"]
    OUT["ВИХІД — генерація<br/>стеля Haiku 4.5 = 64K<br/>~32-64 стор. за виклик<br/>старий max_tokens 8000 = лише ~5-10 стор."]
    NOTE["Очистка одного документа:<br/>на ВХІД зазвичай влазить<br/>на ВИХІД цілий документ НЕ виходить<br/>тому ріжемо на пачки сторінок"]
    IN --> NOTE
    OUT --> NOTE
```

Дослідження щільності міряло **вхід** (скільки Haiku прочитає за раз — ~70 стор.,
`RICH_PASSPORT_MAX_PAGES_DEFAULT`). Проблема очистки — у **виході** (скільки
згенерує). Це різні ліміти; очистку обмежує менший — вихід.

---

## Як обробляється ОДИН документ (пачкове очищення)

```mermaid
flowchart TD
    A["Готовий документ після нарізки/склейки<br/>scanned + .txt + .layout у 02_ОБРОБЛЕНІ"] --> B{"documentNature<br/>= scanned?"}
    B -->|"ні (searchable: DOCX/HTML)"| Z["Пропуск — не чистимо"]
    B -->|так| C["Читаємо .layout посторінково<br/>pages[] з _text + геометрія boundingPoly"]
    C --> D["Рахуємо розмір пачки за щільністю<br/>~1000-2000 ток/стор -> пачка ~8-15 стор<br/>під вихідну стелю і зону якості"]
    D --> E["Ділимо документ на пачки сторінок<br/>межі сторінок зберігаємо"]
    E --> F["ЦИКЛ по пачках сторінок"]
    F --> G["КРОК 1 Конденсатор<br/>детермінований, 0 токенів AI<br/>layout пачки -> чернетка Markdown"]
    G --> H["КРОК 2 AI-поліш Haiku<br/>max_tokens під обсяг пачки до 64K<br/>консервативний, зміст не міняє"]
    H --> I{"stop_reason<br/>= max_tokens?"}
    I -->|"так — обрізало"| J["WARNING + НЕ руйнуємо джерело<br/>зменшити пачку, повтор"]
    J --> F
    I -->|ні| K["Почищений MD пачки + attentionNotes"]
    K --> L{"Ще є пачки?"}
    L -->|так| F
    L -->|ні| M["Склеюємо пачки через<br/>роздільник сторінок ---"]
    M --> N["Долі артефактів по порядку crash-safe:<br/>1 зберегти .md -> 2 updateMeta textFormat=md, cleanedAt<br/>-> 3 .txt у _raw_txt -> 4 .layout видалити"]
    N --> O["Готовий .md<br/>в'ювер показує посторінково (як у Word)"]
```

---

## Де це у потоці всієї обробки (документ за документом)

```mermaid
flowchart TD
    P["Пакет файлів у DP<br/>(фото / скани / PDF)"] --> Q["Нарізка / склейка<br/>splitDocumentsV3"]
    Q --> R["Готові окремі документи у справі<br/>кожен з .txt + .layout у 02"]
    R --> S{"Тумблер 'Очистити для читання'<br/>увімкнено?"}
    S -->|ні| T["Лишаються як .txt"]
    S -->|"так (пост-крок)"| U["ЦИКЛ по готових документах"]
    U --> V["cleanDocument(документ)<br/>= пачкове очищення вище<br/>billAsUserAction=false"]
    V --> W{"Ще документи?"}
    W -->|так| U
    W -->|ні| X["Усі scanned-документи -> .md"]

    R -.->|"ретроактивно, кнопка 3.2"| V2["cleanDocument(один документ)<br/>billAsUserAction=true"]
```

Тумблер у DP і кнопки 3.2/3.3 кличуть **те саме** `cleanDocument` — різниця лише
коли (авто в кінці vs руками) і `billAsUserAction`.

---

## Числа (для розрахунку пачок)

| Параметр | Значення | Джерело |
|---|---|---|
| Щільність кирилиці | ~2 символи / токен | `TASK_smart_triage_passport_scale_and_text.md` |
| Повний юр-текст | ~1000-2000 токенів / стор. | те саме |
| Вхід — вікно Haiku | 200K, зона якості ~120K (~60-80 стор.) | те саме + `RICH_PASSPORT_MAX_PAGES_DEFAULT=70` |
| Вихід — стеля Haiku 4.5 | **64K** (API-ліміт Anthropic) | docs.anthropic.com |
| Старий `max_tokens` | 8000 (~5-10 стор.) — спадок, піднімаємо | `cleanTextService.js:306` |
| Рекомендована пачка | ~8-15 стор. (≈12-30K вихідних токенів) | розрахунок |

---

## Що НЕ робимо

- ❌ НЕ чистимо весь документ одним викликом якщо він > ~30 стор. (обріже на виході).
- ❌ НЕ руйнуємо сирий `.txt`/`.layout` поки очистка не завершилась успішно (і не при обрізанні).
- ❌ НЕ ламаємо межі сторінок (вони потрібні для посторінкового в'ювера).
- ❌ НЕ плутаємо вхідний бюджет (200K, читання) з вихідним (64K, генерація).
- ❌ НЕ ставимо `max_tokens` > 64000 (Haiku 4.5 відхилить запит).
