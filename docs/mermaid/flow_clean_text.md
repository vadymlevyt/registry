# Flow: очищення тексту документа (clean text → Markdown) — TASK 3, смуга C

> Тип: flow + sequence діаграми (тримати синхронно з кодом). Це **алгоритм для
> узгодження ПЕРЕД написанням таск-спеки**. Адвокат дивиться, узгоджує, тоді
> пишемо `docs/tasks/TASK_3_clean_text.md`.

**Дата:** 2026-05-31
**Базовий дизайн:** `docs/consultations/consultation_clean_text_design.md` (SSOT наміру)
**Патерн:** дзеркало `contextGenerator` — ОДНЕ ядро, кілька споживачів (Rule of Three / #11)

---

## Суть у двох реченнях

Сирий OCR-текст (`.txt`) читати незручно: розірвані переноси, злиплі колонки,
сміття розпізнавання. Функція робить з нього **гарний Markdown-документ** (`.md`)
максимально близький до оригіналу — абзаци, заголовки, жирний шрифт, таблиці —
**не змінюючи юридичний зміст** (краще лишити сміття, ніж зіпсувати зміст).

---

## Архітектура: одне ядро — чотири точки виклику

```mermaid
graph TD
    Core["<b>cleanTextService.js</b><br/>СПІЛЬНЕ ЯДРО (stateless, DI)<br/>крок1 конденсатор → крок2 AI-поліш → крок3 fallback"]

    subgraph Споживачі["4 точки виклику — НУЛЬ дублювання логіки"]
        DP["<b>1. Document Processor</b><br/>тумблер «Очистити для читання»<br/>(зараз inline aiCleanText — ПЕРЕНОСИМО в ядро)"]
        Ogляд["<b>2. Кнопка «Очистити тексти» (Огляд)</b><br/>retroactive, всі СИРІ файли справи<br/>прогрес «N з M», пропускає вже .md"]
        Viewer["<b>3. Кнопка «Очистити документ» (Viewer)</b><br/>один відкритий документ"]
        Реєстр["<b>4. Мультивибір у реєстрі</b><br/>1/кілька/всі → очистити вибрані<br/>(+ паралельно мультивидалення)"]
    end

    DP --> Core
    Ogляд --> Core
    Viewer --> Core
    Реєстр --> Core

    Core -.логування.-> Telemetry["ai_usage[] (токени)<br/>+ time_entries[] (час адвоката)<br/>C7 — один шлях на всіх споживачів"]

    style Core fill:#2d4a7c,color:#fff
    style DP fill:#1a3a2a,color:#fff
    style Telemetry fill:#3a2a1a,color:#fff
```

**Ключове рішення (підтверджено адвокатом 2026-05-31):** `aiCleanText`, що зараз
живе inline у `DocumentPipelineContext.jsx` (DP-зачаток), **витягується** у спільне
ядро. DP перестає мати власну копію — стає споживачем #1, як решта.

---

## Ядро: 3-кроковий гібрид (один виклик `cleanDocument`)

```mermaid
flowchart TD
    Start([cleanDocument для одного документа]) --> LoadLayout{"Є .layout.json<br/>на Drive?"}

    LoadLayout -->|"Так (скан через Document AI)"| Step1
    LoadLayout -->|"Ні (DOCX/HTML/старі — тільки .txt)"| LoadTxt["Завантажити сирий .txt"]

    subgraph KROK1["КРОК 1 — Конденсатор (детермінований, 0 токенів AI)"]
        Step1["layout → компактна Markdown-чернетка<br/>• звести розірвані переноси в абзаци<br/>• таблиці → GFM<br/>• викинути шум (колонтитули/номери стор.)<br/>• зберегти порядок читання (колонки)"]
    end

    LoadTxt --> Draft
    Step1 --> Draft["draft (Markdown-чернетка АБО плоский текст)"]

    Draft --> HasKey{"Є API-ключ<br/>Claude?"}
    HasKey -->|Ні| Fallback["Fallback: зберегти draft як є<br/>(deterministic-only, без AI-поліша)<br/>warning «AI недоступний»"]

    subgraph KROK2["КРОК 2 — AI-поліш (Haiku, консервативний)"]
        HasKey -->|Так| Step2["draft → фінальний гарний Markdown<br/>• заголовки, жирний, абзаци, відступи<br/>• максимально близько до оригіналу<br/>⚠ НЕ міняти зміст (цитати/суми/дати/імена)<br/>+ attentionNotes[] (що привернуло увагу)"]
    end

    Step2 --> AIok{"AI повернув<br/>непорожнє?"}
    AIok -->|Ні / помилка| FallbackDraft["Fallback: зберегти draft<br/>warning «очистка не вдалась»"]
    AIok -->|Так| Save

    Fallback --> Save
    FallbackDraft --> Save

    subgraph KROK3["КРОК 3 — Збереження + прибирання артефактів"]
        Save["Записати <basename>_<driveId>.md у 02_ОБРОБЛЕНІ"]
        Save --> MoveTxt["Перемістити старий .txt → 02_ОБРОБЛЕНІ/_raw_txt/<br/>(страховка для звірки, не захаращує реєстр)"]
        MoveTxt --> DelLayout["Видалити .layout.json НАЗАВЖДИ<br/>(паливо відпрацювало: нарізка+очистка)"]
        DelLayout --> Meta["Оновити метадані документа:<br/>textFormat='md', cleanedAt, attentionNotes"]
    end

    Meta --> Done([Готово: повертає {ok, markdown, attentionNotes, warning}])

    style KROK1 fill:#1a3a2a,color:#fff
    style KROK2 fill:#2d4a7c,color:#fff
    style KROK3 fill:#3a2a1a,color:#fff
    style Step2 fill:#2d4a7c,color:#fff
```

**Чому гібрид, а не просто AI (узгоджено):** конденсатор коштує **0 AI-токенів**
(просто CPU), тому гібрид ніколи не дорожчий за чистий AI. Його справжня цінність —
**єдиний токен-доступний спосіб донести структуру layout до AI**: сирий layout у
промпт = втричі дорожче; плоский .txt = втрачені таблиці/колонки. Конденсатор
стискає layout у компактну структуровану чернетку → AI полірує у фінал.

---

## Точка 2: кнопка «Очистити тексти» в Огляді (retroactive, N документів)

```mermaid
flowchart TD
    Click([Адвокат: «Очистити тексти» в Огляді]) --> Scan["Зібрати документи справи з 02_ОБРОБЛЕНІ"]
    Scan --> Filter{"Для кожного документа:<br/>який формат тексту?"}

    Filter -->|"вже .md (очищений)"| Skip["ПРОПУСТИТИ<br/>(не чіпати — захист від подвійної обробки)"]
    Filter -->|"сирий .txt (необроблений)"| Queue["У чергу на очистку"]

    Queue --> Loop["Цикл по черзі: cleanDocument(кожен)<br/>прогрес «Чищу 12 з 43»"]
    Skip --> Loop

    Loop --> Result["ResultCard:<br/>очищено N, пропущено M (вже .md),<br/>помилок K, attentionNotes згруповані"]

    style Skip fill:#3a2a1a,color:#fff
    style Loop fill:#2d4a7c,color:#fff
```

**Змішаний кейс (важливо):** у справі можуть бути і `.md` (додані раніше з
очисткою), і `.txt` (додані без неї). Кнопка в Огляді чіпає **ТІЛЬКИ сирі .txt**,
готові `.md` не перезапускає.

---

## Точка 4: мультивибір у реєстрі (+ мультивидалення)

```mermaid
flowchart TD
    Select(["Адвокат виділяє 1/кілька/всі документи<br/>(чекбокси в реєстрі матеріалів)"]) --> Action{"Яка дія?"}

    Action -->|"«Очистити вибрані»"| CheckEach{"Для кожного вибраного:<br/>вже .md?"}
    CheckEach -->|"так (вже очищений)"| Ask["Перепитати:<br/>«Перезапустити очистку на ньому?»"]
    Ask -->|Так| Reclean["Очистити наново"]
    Ask -->|Ні| SkipDoc["Пропустити цей, решту чистити"]
    CheckEach -->|"ні (сирий)"| Reclean

    Reclean --> CleanResult["Підсумок очистки"]
    SkipDoc --> CleanResult

    Action -->|"«Видалити вибрані»"| DelConfirm["Підтвердження видалення"]
    DelConfirm --> DelEach["Для кожного: повне видалення<br/>• файл у 01_ОРИГІНАЛИ (Drive)<br/>• .txt + .layout.json (якщо є)<br/>• .md (якщо очищений)<br/>• метадані документа з реєстру"]
    DelEach --> DelResult["Підсумок видалення"]

    style Reclean fill:#2d4a7c,color:#fff
    style DelEach fill:#5a2a2a,color:#fff
```

**Примітка:** мультивидалення — окрема дія, прикручена «попутно» (раз робимо
мультивибір у реєстрі — логічно додати і пакетне видалення). Зараз видалення —
по одному; стане 1/кілька/всі.

---

## Долі артефактів (зведена таблиця)

| Артефакт | До очистки | Після очистки | Чому |
|----------|-----------|---------------|------|
| `<basename>_<id>.md` | — | **створюється** у 02_ОБРОБЛЕНІ | viewer показує його; фінальний гарний документ |
| `<basename>_<id>.txt` (сирий) | у 02_ОБРОБЛЕНІ | **переміщається** у `02_ОБРОБЛЕНІ/_raw_txt/` | страховка для звірки, не захаращує реєстр (дефолт, переглядається пізніше) |
| `<basename>_<id>.layout.json` | у 02_ОБРОБЛЕНІ | **видаляється назавжди** | паливо відпрацювало (нарізка + конденсатор); метадані вже використані |
| метадані документа | `textFormat='txt'` | `textFormat='md'`, `cleanedAt`, `attentionNotes[]` | viewer/контекст-генератор/агент читають «.md якщо є, інакше .txt» |

---

## Що TASK 3 НЕ робить (межі — у спеці деталізуємо)

- ❌ re-OCR одного документа за сумнівом (§5 дизайну) — концептуально закладаємо,
  але реалізація — наступний крок (борг).
- ❌ Не чіпає `DpImageMergeEditor`, `ImageMergePanel`, `App.jsx` (паралельні смуги B/A).
- ❌ Не змінює промпт `contextGenerator` (окремий сервіс).

---

## Відкриті деталі для спеки (не для діаграми)

1. Як саме конденсатор читає **реальний** Document AI `pageStructure` (Google-shape
   `paragraphs/blocks/tables/layout`) — зламаний leftback очікує інший формат
   `{type,text}`; потрібен адаптер або переписаний reader + відсутні хелпери в `ocrService.js`.
2. Точний `agentType` для `resolveModel` (новий `text_cleaner`?) і `logAiUsage` context.
3. Білінг: чи нараховувати DP-шлях (автоматичний) інакше ніж кнопку Огляду (дія адвоката).
4. Чи viewer уже рендерить `.md` (перевірити `DocumentViewer`).

---

**Це алгоритм для узгодження. Після «ок» від адвоката → пишемо `docs/tasks/TASK_3_clean_text.md`.**
