# Регресія нарізки DP — суть проблеми і як лікувати (для адвоката)

**Дата:** 2026-06-02 (оновлено: адвокат уточнив — нестиснені файли теж дають 1 документ)
**Призначення:** пояснити простими словами ДВІ окремі проблеми DP і як їх лікувати.
**Повне технічне розслідування:** `docs/diagnostics/diagnostic_clean_text_pipeline_regression_FINDINGS.md`

> **Головне:** усі файли — це **скани (зображення), запечені в PDF**. Симптом єдиний:
> система **перестала різати том на сторінки/документи** — на вхід розпізнавачу йде
> завеликий або суцільний блок замість маленьких посторінкових шматків. Звідси два
> прояви: великий файл → «більше 40 МБ» (падіння); решта → 1 документ (вхід=вихід).
> Нова «очистка тексту» НЕ винна. Код нарізки/розпізнавання **не змінювався**.

> ⚠ **ЧЕСНО про мої попередні версії — усі три були неповні/хибні:**
> «floor 5 сторінок» (хибна математика), «спільні ресурси від стиснення» (не тримається
> для простих сканів — адвокат справедливо вказав: downscale зображень не створює
> «спільних ресурсів»). **Я НЕ можу назвати точну причину зі статичного коду** — бо
> код не змінювався, отже причина в РАНТАЙМІ, а пісочниця не має ваших файлів/Drive/API,
> щоб це відтворити. Нижче — чесна картина і що саме треба запустити, щоб назвати причину.

---

## Картина 1 — ЯК МАЛО Б ПРАЦЮВАТИ

```mermaid
flowchart TD
    A["Сканований том"] --> B["Розпізнати текст<br/>посторінково (OCR)"]
    B --> C["AI-РІЗНИК дивиться на<br/>посторінковий 'паспорт' тому"]
    C --> D["Знаходить межі:<br/>де закінчується документ 1,<br/>починається документ 2..."]
    D --> E["Том порізано на<br/>багато документів"]
    E --> F["ОБРОБЛЕНІ: текст+layout<br/>кожного документа"]

    classDef ok fill:#e8f5e9,stroke:#2e7d32,color:#1b5e20;
    class A,B,C,D,E,F ok;
```

---

## Картина 2 — ЄДИНИЙ СИМПТОМ: нарізка не відбувається

```mermaid
flowchart TD
    IN["Скан-том у PDF<br/>(зображення на сторінках)"] --> CUT{"Система має порізати<br/>на маленькі посторінкові шматки"}

    CUT -->|"раніше — працювало"| OK["✅ маленькі шматки →<br/>розпізнавання → межі →<br/>багато документів"]

    CUT -->|"зараз — НЕ ріже"| BAD["⚠ на вхід розпізнавачу йде<br/>завеликий / суцільний блок"]
    BAD --> R1["Великий файл →<br/>блок &ge; 40 МБ →<br/>'більше 40 МБ' (падіння)"]
    BAD --> R2["Решта файлів →<br/>1 суцільний документ<br/>(вхід=вихід, ОБРОБЛЕНІ порожні)"]

    classDef ok fill:#e8f5e9,stroke:#2e7d32,color:#1b5e20;
    classDef bad fill:#ffebee,stroke:#c62828,color:#b71c1c;
    classDef q fill:#fff8e1,stroke:#f9a825,color:#795548;
    class OK ok;
    class BAD,R1,R2 bad;
    class CUT q;
```

---

## Картина 3 — ЧЕСНО: ЩО ДОВЕДЕНО vs ЩО ТРЕБА ЗАПУСТИТИ

```mermaid
flowchart TD
    subgraph PROVEN["✅ ДОВЕДЕНО (статично, по коду)"]
        P1["Очистка тексту — НЕ винна"]
        P2["Код нарізки/розпізнавання/AI-різника<br/>НЕ змінювався з робочого стану"]
        P3["Отже причина — у РАНТАЙМІ,<br/>а не в зміні коду"]
    end

    subgraph CONFIRM["❓ ТРЕБА ПІДТВЕРДИТИ 1 ЗАПУСКОМ (кандидати)"]
        C1["Кандидат 1: pdf-lib неправильно<br/>читає кількість сторінок цього PDF<br/>→ чанк виходить на весь файл"]
        C2["Кандидат 2: вирізаний шматок<br/>не зменшується (структура PDF<br/>від репакера) → блок завеликий"]
        C3["Кандидат 3: розпізнавання дає<br/>порожній/неповний посторінковий текст<br/>→ AI-різник бачить 1 суцільний блок"]
    end

    PROVEN --> NEED["Без ваших файлів + Drive + API<br/>пісочниця НЕ відтворює рантайм.<br/>Потрібен 1 запуск з логами."]
    CONFIRM --> NEED

    classDef good fill:#e8f5e9,stroke:#2e7d32,color:#1b5e20;
    classDef q fill:#fff8e1,stroke:#f9a825,color:#795548;
    classDef need fill:#e3f2fd,stroke:#1565c0,color:#0d47a1;
    class P1,P2,P3 good;
    class C1,C2,C3 q;
    class NEED need;
```

> **Я більше НЕ вгадую механізм.** Тричі назвав конкретну причину (floor / спільні
> ресурси) — і двічі помилився, бо без живого запуску це лише здогад. Чесна позиція:
> причина в рантаймі (один із 3 кандидатів вище), і назвати точно можна ЛИШЕ
> прогоном одного вашого файлу з діагностичними логами (чанк-байти, кількість сторінок,
> чи є посторінковий текст). Це і є наступний крок.

---

## Картина 4 — ХТО НЕ ВИНЕН

```mermaid
flowchart LR
    Q["Чому 'після очистки тексту'?"] --> C1["Очистка тексту коду<br/>нарізки не торкається = НЕВИННА"]
    Q --> C2["Код нарізки/розпізнавання/різника<br/>НЕ змінювався = не помилка коду"]
    Q --> C3["Проблема A: змінився файл (стиснення)<br/>Проблема B: рантайм-поведінка AI на томах"]

    classDef good fill:#e8f5e9,stroke:#2e7d32,color:#1b5e20;
    classDef neutral fill:#e3f2fd,stroke:#1565c0,color:#0d47a1;
    class C1,C2 good;
    class C3 neutral;
```

---

## Картина 5 — ЯК ЛІКУВАТИ (спершу — назвати причину)

```mermaid
flowchart TD
    STEP1["КРОК 1 — ДІАГНОЗ (обовʼязковий):<br/>додати тимчасові логи і ПРОГНАТИ 1 ваш файл<br/>у реальному застосунку. Лог покаже:<br/>• скільки сторінок побачив pdf-lib<br/>• яка реальна вага кожного шматка<br/>• чи є посторінковий текст від розпізнавання"]

    STEP1 --> WHICH{"Що показав лог?"}
    WHICH -->|"чанк реально &ge; 40 МБ"| FIXA["Фікс: перевіряти вагу шматка,<br/>завеликий → ділити далі / в зображення"]
    WHICH -->|"кількість сторінок невірна"| FIXB["Фікс: надійне читання сторінок<br/>цього виду PDF (інший парсер)"]
    WHICH -->|"текст порожній/неповний"| FIXC["Фікс: гарантувати повну посторінкову<br/>карту до AI-різника"]

    FIXA --> R["✅ Томи знову нарізаються<br/>+ очистка тексту зберігається"]
    FIXB --> R
    FIXC --> R

    WORK["Обхід ПОКИ що:<br/>різати том на частини по ~70-100 стор.<br/>і подавати окремо"] --> R

    classDef step fill:#e3f2fd,stroke:#1565c0,color:#0d47a1;
    classDef fix fill:#e8f5e9,stroke:#2e7d32,color:#1b5e20;
    classDef res fill:#c8e6c9,stroke:#1b5e20,color:#1b5e20;
    class STEP1,WHICH step;
    class FIXA,FIXB,FIXC,WORK fix;
    class R res;
```

---

## Підсумок словами адвоката

| Питання | Чесна відповідь |
|---|---|
| Яка точна причина? | **Поки не названа.** Код не змінювався → причина в рантаймі. Тричі вгадував — двічі помилився. Більше не вгадую. |
| Чи винна очистка тексту? | **Ні.** Доведено — коду нарізки не торкається. |
| Чи це помилка в коді нарізки? | Код нарізки/розпізнавання/AI-різника **не змінювався** з робочого стану. Симптом — рантайм. |
| Чому ж тоді ламається? | Один із 3 кандидатів: невірна к-сть сторінок / шматок не зменшується / порожній посторінковий текст. Який — покаже лог. |
| Що треба, щоб назвати точно? | **1 запуск вашого файлу з діагностичними логами** в реальному застосунку. Пісочниця цього не може (нема файлів/Drive/API). |
| Обхід зараз? | Різати том на частини по ~70-100 сторінок і подавати окремо. |
