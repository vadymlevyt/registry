# Регресія нарізки DP — суть проблеми і як лікувати (для адвоката)

**Дата:** 2026-06-02
**Призначення:** пояснити простими словами, ЧОМУ ті самі файли раптом
перестали нарізатись, і ЯК це полагодити. Без інженерного жаргону.
**Повне технічне розслідування:** `docs/diagnostics/diagnostic_clean_text_pipeline_regression_FINDINGS.md`

> **Головне в одному реченні:** нова «очистка тексту» НЕ винна — винна стара
> прихована вада в тому, як система рахує розмір пакета сторінок для
> розпізнавання; вона була завжди, але «вистрілила» саме на важких сканах.

---

## Картина 1 — ЩО МАЛО Б ВІДБУВАТИСЬ (нормальна робота)

```mermaid
flowchart TD
    A["Адвокат закидає пачку<br/>сканованих PDF"] --> B["Діловод ділить пачку<br/>на ПАКЕТИ сторінок"]
    B --> C["Кожен ПАКЕТ не більше 40 МБ<br/>відправляє на розпізнавання<br/>(сервіс Google)"]
    C --> D["Отримано текст<br/>кожної сторінки"]
    D --> E["AI бачить, де закінчується<br/>один документ і починається інший"]
    E --> F["Пачка порізана на багато<br/>окремих документів"]
    F --> G["ОРИГІНАЛИ: цілий файл<br/>ОБРОБЛЕНІ: текст кожного документа"]

    classDef ok fill:#e8f5e9,stroke:#2e7d32,color:#1b5e20;
    class A,B,C,D,E,F,G ok;
```

---

## Картина 2 — ЩО ЛАМАЄТЬСЯ ЗАРАЗ (дві окремі поломки)

```mermaid
flowchart TD
    START["Пачка важких сканів<br/>(сторінки по 8+ МБ)"] --> SIZE{"Скільки сторінок<br/>класти в один пакет?"}

    SIZE --> RULE["Рахунок за СЕРЕДНЬОЮ вагою<br/>+ жорстке правило:<br/>'не менше 5 сторінок'"]
    RULE --> BAD1["5 важких сторінок<br/>= понад 40 МБ"]

    BAD1 --> CRASH{"Сервіс розпізнавання"}
    CRASH -->|"відмова:<br/>'файл більший за 40 МБ'"| FAIL1["❌ ПОЛОМКА 1<br/>уся робота падає<br/>('Потребує уваги')"]

    FAIL1 -.->|"якщо текст так і не отримано"| NOTEXT["Тексту немає"]
    NOTEXT --> AICUT{"AI шукає межі<br/>між документами"}
    AICUT -->|"по чому різати?<br/>тексту нема"| GIVEUP["AI здається:<br/>'це все один документ'"]
    GIVEUP --> FAIL2["❌ ПОЛОМКА 2<br/>вийшов 1 документ<br/>ОБРОБЛЕНІ порожні<br/>файл лише в ОРИГІНАЛАХ"]

    classDef bad fill:#ffebee,stroke:#c62828,color:#b71c1c;
    classDef warn fill:#fff8e1,stroke:#f9a825,color:#795548;
    class FAIL1,FAIL2 bad;
    class RULE,BAD1,GIVEUP,NOTEXT warn;
```

**Звідки взялося «правило не менше 5».** Колись його додали, щоб не дробити роботу
на безліч крихітних пакетів (це сповільнює). Але ніхто не передбачив випадок, коли
сторінки настільки важкі, що навіть 5 уже не влазять у 40 МБ. Тоді «мінімум 5»
перемагає головну межу «40 МБ» — і ламає її.

---

## Картина 3 — ХТО НЕ ВИНЕН (важливо для довіри)

```mermaid
flowchart LR
    Q["Чому зламалось<br/>'після очистки тексту'?"] --> CHECK{"Перевірка:<br/>чи очистка тексту<br/>торкається нарізки?"}
    CHECK -->|"НІ — доведено"| INNOCENT["Очистка тексту = НЕВИННА<br/>код нарізки/розпізнавання<br/>вона навіть не зачіпає"]
    CHECK -->|"А що тоді?"| REAL["Стара прихована вада<br/>у рахунку розміру пакета<br/>(була завжди, не нова)"]

    INNOCENT --> WHY["'Після очистки' = збіг у часі,<br/>а не причина"]

    classDef good fill:#e8f5e9,stroke:#2e7d32,color:#1b5e20;
    classDef neutral fill:#e3f2fd,stroke:#1565c0,color:#0d47a1;
    class INNOCENT,WHY good;
    class REAL,Q,CHECK neutral;
```

---

## Картина 4 — ЯК ЛІКУВАТИ (план фіксу)

```mermaid
flowchart TD
    subgraph CURE["ЛІКУВАННЯ (наступна сесія)"]
        FIX1["1. Зробити межу 40 МБ ГОЛОВНОЮ:<br/>важкий пакет ділити дрібніше<br/>навіть менше 5 сторінок"]
        FIX2["2. Не валити всю роботу<br/>через один важкий пакет:<br/>розділити його навпіл і обробити"]
        FIX3["3. Додати запас (цілитись у ~32 МБ),<br/>щоб нерівні сторінки<br/>не пробивали межу"]
        FIX4["4. Прибрати 'німі' помилки запису:<br/>якщо щось не зберіглось у ОБРОБЛЕНІ —<br/>писати в лог, а не мовчати"]
    end

    FIX1 --> RESULT
    FIX2 --> RESULT
    FIX3 --> RESULT
    FIX4 --> RESULT
    RESULT["✅ Важкі скани знову<br/>нарізаються на багато документів<br/>+ очистка тексту і нумерація<br/>версій ЗБЕРІГАЮТЬСЯ"]

    NOTE["⚠ Відкат назад НЕ робимо:<br/>проблемний код старий, не привнесений<br/>новими функціями. Відкат не полагодить,<br/>а зруйнує очистку тексту."]

    classDef fix fill:#e8f5e9,stroke:#2e7d32,color:#1b5e20;
    classDef res fill:#c8e6c9,stroke:#1b5e20,color:#1b5e20;
    classDef note fill:#fff8e1,stroke:#f9a825,color:#795548;
    class FIX1,FIX2,FIX3,FIX4 fix;
    class RESULT res;
    class NOTE note;
```

---

## Підсумок словами адвоката

| Питання | Відповідь |
|---|---|
| Чи винна нова «очистка тексту»? | **Ні.** Доведено — вона не торкається нарізки. |
| Що ж тоді зламалось? | Стара прихована вада: правило «мінімум 5 сторінок у пакеті» перемагає межу «40 МБ» на важких сканах. |
| Чому раптом саме зараз? | Вада була завжди; «вистрілила» на конкретних важких файлах. Точний тригер підтвердить один тестовий прогін. |
| Що буде з очисткою тексту? | **Зберігається повністю.** Лікуємо лише старий рахунок розмірів. |
| Чи треба щось відкочувати? | **Ні.** Відкат не допоможе і зашкодить. Потрібен прицільний фікс. |
