# Регресія нарізки DP — суть проблеми і як лікувати (для адвоката)

**Дата:** 2026-06-02 (оновлено після уточнення адвоката про стиснення файлу)
**Призначення:** пояснити простими словами, ЧОМУ ті самі файли раптом
перестали нарізатись, і ЯК це полагодити. Без інженерного жаргону.
**Повне технічне розслідування:** `docs/diagnostics/diagnostic_clean_text_pipeline_regression_FINDINGS.md`

> **Головне в одному реченні:** файл стиснули зовнішнім інструментом
> (200 МБ → 79 МБ), і це **зламало нарізку на сторінки** — тепер «вирізати
> кілька сторінок» дає шматок майже на весь файл. Нова «очистка тексту» НЕ винна,
> помилки в коді теж немає — змінився сам файл усередині.

> ⚠ **Поправка до першої версії цієї схеми.** Спершу я припустив «5 важких
> сторінок по 8 МБ = 40 МБ». Адвокат справедливо вказав: файл 79 МБ / 335 сторінок
> = лише 0,24 МБ на сторінку, така математика не сходиться. Правильна причина —
> стиснення (нижче).

---

## Картина 1 — ЩО МАЛО Б ВІДБУВАТИСЬ (нормальна робота)

```mermaid
flowchart TD
    A["Адвокат закидає<br/>сканований PDF (том справи)"] --> B["Система ВИРІЗАЄ<br/>по 25 сторінок у пакет"]
    B --> C["Кожен пакет маленький<br/>(не більше 40 МБ)<br/>і йде на розпізнавання"]
    C --> D["Отримано текст<br/>кожної сторінки"]
    D --> E["AI бачить межі<br/>між документами"]
    E --> F["Том порізано на багато<br/>окремих документів"]
    F --> G["ОРИГІНАЛИ: цілий файл<br/>ОБРОБЛЕНІ: текст + layout<br/>кожного документа"]

    classDef ok fill:#e8f5e9,stroke:#2e7d32,color:#1b5e20;
    class A,B,C,D,E,F,G ok;
```

---

## Картина 2 — ЧОМУ СТИСНЕННЯ ЛАМАЄ НАРІЗКУ (суть)

Нарізка вирізає сторінки, **копіюючи їх разом з усіма ресурсами**
(шрифти, зображення, спільні об'єкти), на які ці сторінки посилаються.

```mermaid
flowchart LR
    subgraph BEFORE["ДО стиснення — 200 МБ — ПРАЦЮВАЛО"]
        B1["Кожна сторінка<br/>несе СВІЙ вміст"]
        B2["Вирізаємо 25 сторінок"]
        B3["→ маленький шматок ~6 МБ"]
        B1 --> B2 --> B3
    end

    subgraph AFTER["ПІСЛЯ стиснення — 79 МБ — ЛАМАЄТЬСЯ"]
        A1["Компресор зробив сторінки<br/>СПІЛЬНИМИ за ресурсами"]
        A2["Вирізаємо 25 сторінок"]
        A3["→ тягне за собою МАЙЖЕ ВЕСЬ файл<br/>шматок ~40-79 МБ"]
        A1 --> A2 --> A3
    end

    classDef ok fill:#e8f5e9,stroke:#2e7d32,color:#1b5e20;
    classDef bad fill:#ffebee,stroke:#c62828,color:#b71c1c;
    class B1,B2,B3 ok;
    class A1,A2,A3 bad;
```

---

## Картина 3 — ДВА ПРОЯВИ ОДНІЄЇ ПРИЧИНИ

```mermaid
flowchart TD
    ROOT["ПРИЧИНА:<br/>стиснення зробило, що нарізка<br/>не зменшує розмір шматка"]

    ROOT --> BIG["Великий файл 79 МБ"]
    ROOT --> SMALL["Менші файли 18-42 МБ"]

    BIG --> BIGR["Шматок &ge; 40 МБ →<br/>сервіс відмовляє:<br/>'файл більший за 40 МБ'"]
    BIGR --> BIGF["❌ Уся робота падає<br/>('Потребує уваги')"]

    SMALL --> SMR["Шматок не зменшився,<br/>але під 40 МБ →<br/>йде одним суцільним блоком"]
    SMR --> SMF["❌ На виході 1 документ<br/>(вхід = вихід)<br/>ОБРОБЛЕНІ порожні<br/>(немає .txt і layout)"]

    classDef root fill:#fff8e1,stroke:#f9a825,color:#795548;
    classDef bad fill:#ffebee,stroke:#c62828,color:#b71c1c;
    class ROOT root;
    class BIGF,SMF,BIGR,SMR bad;
```

---

## Картина 4 — ХТО НЕ ВИНЕН (важливо для довіри)

```mermaid
flowchart LR
    Q["Чому зламалось<br/>'після очистки тексту'?"] --> C1["Очистка тексту<br/>коду нарізки не торкається<br/>(доведено) = НЕВИННА"]
    Q --> C2["Код нарізки НЕ змінювався<br/>з робочого стану<br/>(доведено) = не помилка коду"]
    Q --> C3["Змінився САМ ФАЙЛ:<br/>його стиснули =<br/>СПРАВЖНЯ причина"]

    C3 --> PROOF["Перевірка за 1 прогін:<br/>дати ОРИГІНАЛ 200 МБ →<br/>має нарізатись як раніше"]

    classDef good fill:#e8f5e9,stroke:#2e7d32,color:#1b5e20;
    classDef neutral fill:#e3f2fd,stroke:#1565c0,color:#0d47a1;
    class C1,C2 good;
    class C3,PROOF neutral;
```

---

## Картина 5 — ЯК ЛІКУВАТИ

```mermaid
flowchart TD
    subgraph NOW["ЗАРАЗ (обхід для адвоката)"]
        W1["Не стискати том ПЕРЕД нарізкою<br/>зовнішнім інструментом —<br/>давати оригінал, система сама<br/>ріже великі файли"]
    end

    subgraph CODE["ФІКС У КОДІ (наступна сесія)"]
        F1["1. Після вирізання — ПЕРЕВІРЯТИ<br/>реальну вагу шматка.<br/>Якщо нарізка не зменшила —<br/>не валити роботу"]
        F2["2. Якщо шматок завеликий:<br/>ділити далі / перетворювати<br/>сторінки на зображення<br/>(обхід спільних ресурсів)"]
        F3["3. Чесне повідомлення адвокату:<br/>'цей файл стиснутий так, що<br/>не ріжеться — дайте оригінал',<br/>а не німа помилка"]
    end

    W1 --> RESULT
    F1 --> RESULT
    F2 --> RESULT
    F3 --> RESULT
    RESULT["✅ Том знову нарізається на документи<br/>+ очистка тексту і версії ЗБЕРІГАЮТЬСЯ"]

    classDef now fill:#e3f2fd,stroke:#1565c0,color:#0d47a1;
    classDef fix fill:#e8f5e9,stroke:#2e7d32,color:#1b5e20;
    classDef res fill:#c8e6c9,stroke:#1b5e20,color:#1b5e20;
    class W1 now;
    class F1,F2,F3 fix;
    class RESULT res;
```

---

## Підсумок словами адвоката

| Питання | Відповідь |
|---|---|
| Чому математика «5 сторінок = 40 МБ» не сходилась? | Бо вона була неправильна. 79 МБ / 335 стор. = 0,24 МБ/стор. Реальна причина інша — стиснення. |
| Що насправді зламалось? | Нарізка вирізає сторінки копіюванням. Після стиснення сторінки «зрослись» спільними ресурсами — вирізаєш кілька, а тягнеш майже весь файл. |
| Чому раніше (200 МБ) працювало? | Нестиснений файл мав незалежні сторінки — вирізання давало справді маленькі шматки. |
| Чи винна очистка тексту? | Ні. Доведено — не торкається нарізки. |
| Чи це помилка в коді? | Ні. Код нарізки не змінювався. Змінився сам файл (стиснення). |
| Що робити зараз? | Дати системі ОРИГІНАЛ (нестиснений) — вона сама ріже великі файли. Це ж підтвердить діагноз. |
| Що полагодити в коді? | Перевіряти реальну вагу шматка після нарізки; якщо не зменшився — ділити далі або переводити в зображення, а не валитись. |
