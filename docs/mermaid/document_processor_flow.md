# Document Processor — повний запланований флоу (схема)

**Дата:** 2026-05-29
**Призначення:** візуальна мапа DP (візія + поточний стан). Рендериться на GitHub
і в будь-якому mermaid-переглядачі.
**Повʼязане:** `docs/consultations/consultation_dp_product_vision.md`,
`docs/consultations/consultation_dp_flow_observations.md` (спостереження адвоката).

> Виправлено після зауваження адвоката (2026-05-29): гілка тумблера
> «Просто додати файли» **оминає** крок правки плану — див. спостереження #1.

```mermaid
flowchart TD
    subgraph Z1["ЗОНА 1 · Вхідна"]
        IN["Адвокат закидає суміш:<br/>фото, PDF, DOCX, HEIC, ZIP<br/>з пристрою / Drive / 00_INBOX"]
    end

    subgraph Z2["ЗОНА 2 · Налаштування"]
        ACT["ОСНОВНІ ДІЇ:<br/>розпакувати · конвертувати ·<br/>склеїти фото · нарізати PDF"]
        MOD["MODIFIER'и:<br/>стиснути · очистити текст ·<br/>розкласти по провадженнях ·<br/>case_context · картка · дедлайни"]
        TOG["Toggle 'Просто додати файли'"]
    end

    IN --> START(["Розпочати обробку"])
    ACT --> START
    MOD --> START
    TOG --> START

    START --> ROUTE{"Вибір сценарію НА ВХОДІ<br/>нуль AI, за MIME / text-layer"}

    %% ZIP
    ROUTE -->|"ZIP"| UNPACK["Розпакувати → файли назад у вибір"]
    UNPACK -.-> ROUTE

    %% ── ТУМБЛЕР ON — ОМИНАЄ ПЛАН (спостереження №1) ──
    ROUTE -->|"toggle ON:<br/>просто додати"| OCRS["OCR + метадані<br/>кожен файл = 1 документ"]
    OCRS --> SUMM["Підсумок: 'що додано'<br/>лише звіт, БЕЗ правки плану"]

    %% ── ФОТО ──
    ROUTE -->|"усі фото"| IMG["Image-конвеєр:<br/>OCR · сортування ·<br/>групування у документи (AI)"]

    %% ── PDF нарізка ──
    ROUTE -->|"усі PDF (toggle OFF)"| TRI["AI Triage — межі документів"]
    ROUTE -->|"мікс типів"| TRI

    %% ── ВІДКРИТЕ ПИТАННЯ: стискання ПЕРЕД OCR (спостереження №2) ──
    COMP["ВІДКРИТЕ ПИТАННЯ:<br/>стискання ПЕРЕД OCR?<br/>меншому файлу легше розпізнаватись<br/>(заміряти перед рішенням)"]
    COMP -.-> TRI
    COMP -.-> IMG

    TRI --> OCR["OCR (Document AI)<br/>+ конвертація форматів"]
    OCR --> PLAN["AI пропонує ПЛАН:<br/>межі · назви · типи · фрагменти"]
    IMG --> PLAN

    %% ── ГЕЙТ ПРАВКИ ПЛАНУ (тільки для фото/нарізки, НЕ для тумблера) ──
    subgraph Z3["ЗОНА 3 · Адвокат-диригент править ПЛАН"]
        PLAN --> EDIT["Дерево пропозицій:<br/>rename · тип · обʼєднати/розділити ·<br/>drag фото між групами · crop/rotate · дублі"]
        EDIT --> CONFIRM{"виконати чи правити далі?"}
        CONFIRM -->|"правити"| EDIT
    end

    CONFIRM -->|"Виконати"| EXEC["PERSIST за маршрутом:<br/>add_as_is / slice / image_merge /<br/>fragment_reconstruct / to_fragments"]
    EXEC --> CRT["Завантаження на Drive +<br/>createDocument + add_documents"]

    %% ── MODIFIER'и (після створення документів) ──
    CRT --> APPLYMOD["Застосувати MODIFIER'и:<br/>стиснути · очистити · розкласти ·<br/>резюме · дедлайни · case_context · картка"]
    SUMM --> APPLYMOD

    APPLYMOD --> RES["РЕЗУЛЬТАТ у справі:<br/>документи по провадженнях ·<br/>case_context оновлено · дедлайни в календарі"]

    classDef gate fill:#3a1e2f,stroke:#f472b6,color:#fce7f3;
    classDef question fill:#3a2f1e,stroke:#fbbf24,color:#fef3c7;
    classDef bypass fill:#1e3a2f,stroke:#34d399,color:#d1fae5;
    class PLAN,EDIT,CONFIRM gate;
    class COMP question;
    class OCRS,SUMM bypass;
```

## Легенда

- **Зелене (тумблер ON)** — «Просто додати файли»: оминає правку плану, кожен файл
  лягає як є, лише підсумок «що додано». Жодного редагування — у цьому й сенс тумблера.
- **Рожеве** — гейт «адвокат править план». Реальний зараз тільки для **фото** (1B);
  для **нарізки PDF** — Фаза 5 (зараз автопідтвердження).
- **Жовте** — відкрите питання: чи варто стискати файли **перед** OCR (спостереження #2).

## Ключові принципи

1. **Cheap before Expensive** — сценарій обирається на вході без AI (за типом файлу).
   AI вмикається лише ВСЕРЕДИНІ сценарію (групування фото, межі PDF).
2. **Тумблер «Просто додати» = довіра** — оминає будь-яку правку, додає як є.
3. **Гейт правки плану** — серце візії; для нарізки PDF досі не реалізований (Фаза 5).
4. **Modifier'и** застосовуються ПІСЛЯ створення документів (а можливо частина —
   стискання — має переїхати на вхід; спостереження #2).
